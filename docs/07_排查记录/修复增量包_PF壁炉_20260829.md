# 修复增量包 & 全量归档 交付记录 · 2026-08-29

配套 `Player.log冷启动排查_石化线v3_20260829.md`（后者留指针）。记录 post-1935 两项修复的打包交付。

## 变更范围（1935 全量归档 vs 当前部署 Mods 全量 hash diff）

净变更仅 2 个真实文件（unified.xml=运行时生成、nunit_*.tmp=垃圾，均排除；removed=0 说明 A–H/G 都已在 1935 基线内）：

1. `HSK工业科研大修/1.6/Assemblies/ProcessorFramework.dll` —— 回退到 08-29 05:17 Cecil "DefOf" 改写**前**的干净版（SHA256 前16 `3e24319ecfca8f3e`）。修：打开小人管理面板即 `MissingFieldException: ActiveProcess Verse.Thing.def`（OnGUI 每帧抛 → 小人卡死）。
2. `鼠族家具拓展/Defs/RKFC_JoyTemp.xml` —— 壁炉火焰落点 fireSize 1.4→0.9 + offset (0,0,-0.15)。

## 增量修复包（主会话已交付：Downloads\HSK_FixPack_20260829.zip）

（本任务仅核验其内容=bat 启动器 + ps1 逻辑 + files/ 两文件 + README，未重复构建。）

- `apply_fix.bat` = 3 行纯 ASCII 启动器：`powershell -NoProfile -ExecutionPolicy Bypass -File "%~dp0apply_fix.ps1" %*` + 退出码透传 + `pause`。
- `apply_fix.ps1` = 全部逻辑（PowerShell 原生 Unicode，`Copy-Item -LiteralPath`，中文 mod 路径零坑）：探测 Mods（参数 > 注册表 SteamPath > 常见 Steam 路径）→ 逐文件把将被覆盖的原件备份到 `backup_<时间戳>\`（**保留相对子目录**）→ 复制 → 打印 `[OK]/[FAIL]` 汇总。
- `files\` 保持 Mods 相对路径；`README.txt`（UTF-8）。
- 已交付 zip：`Downloads\HSK_FixPack_20260829.zip`（含上述 bat+ps1+files+README，zip 内 ProcessorFramework.dll 核对=`3e24319` 干净）。

**止损定论**：cmd 批处理搞中文路径是天坑（源中文字面量吞相邻 ASCII；`copy <中文> >nul` 静默不复制；块内 `%` 解析期展开；延迟展开+括号嵌套互斥）。正解=bat 只做启动器，逻辑交 PowerShell。自测通过：假 Mods（两个中文子目录 + 预置 OLD 原件）→ 部署成 NEW（PF `3e24319`/RKFC 18065）、backup_ 里两 OLD 且保留相对子目录、`[OK]×2 Done 2`、rc=0。

## 全量归档（含全部修复终态）

`RimWorld_Mods_v3_20260829_2155.7z` = 1,266,191,108 B（1.18 GiB，压 51.5%，58122 文件，内 PF.dll 核对 = `3e24319` 干净，`7z t` 过）+ 900MB 分卷 `_2155_split.7z.001`(900 MiB)/`.002`(308 MiB)；均已复制 Downloads。旧 `1935` 全量含 corrupt PF.dll，隔离于 `outputs\SUPERSEDED_corruptPF_1935\`，**勿发**。
