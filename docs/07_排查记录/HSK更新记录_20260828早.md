# Hardcore SK 差分更新记录(2026-08-28 早间包)

> 差分包 `C:\Users\admin\Downloads\Hardcore-SK-20260828早上差分\Mods\`,2054 文件 / 8 个上游 mod。逐文件 md5 比对后只替换真正不同的文件,部署端独有文件一律保留。上一批(08-27 差分 + Vile)见 `HSK更新记录_20260828.md`。

## 1. 结果概览

| mod | 差分包文件 | 实际替换 | 说明 |
|---|---|---|---|
| CombatExtended | 2027 | **2026** | 2024 个文件只差行尾(上游新增 `.gitattributes` 做 LF 归一);真实变更 = `CombatExtended.dll` 重编译 + 中文补译 4999 键 + `Hediffs_CE.xml` 末尾换行 |
| Core_SK | 18 | **3** | `Core_SK.dll`(重编译)、`Buildings_Production_Furnaces.xml`、`Building_FishingPier.xml`;其余 15 与部署逐字节一致 |
| HMC Vile's Pre-Industrial | 1 | **1** | `Headwear_Patch.xml` 仅行尾归一,内容等价 |
| Core_SK_CoreModule / _DLCModule / CyberneticOrganism_SK / RatkinRaceHSK | 1 / 4 / 2 / 1 | 0 | 全部与部署一致,未动 |

未部署:`CombatExtended/.gitattributes`(仓库簿记文件,非 mod 内容)。
部署端独有且保留:CE 1817 个文件(本地补译/其他语言等)、Core_SK 10811 个文件 —— 差分包是增量,**不可据此删部署文件**。
核对终态:`_tmp/hsk_verify_20260828.py` → 2053/2054 逐字节一致,余 1 = 有意跳过的 `.gitattributes`。

## 2. 语义变更(只有 2 处会影响游戏)

1. **`Kiln`(窑)删除 `<inspectorTabs><li>ITab_Bills</li></inspectorTabs>`**(Buildings_Production_Furnaces.xml)。同文件其余差异是空格→制表符、`/>` 前空格。已核对本地无 xpath 依赖该节点:`HSK工业科研大修/Patch_Ceramics_Glasswork.xml` 只碰 `RecipeDef/recipeUsers`,Vile's Metallurgy 对 `Kiln` 是整 def Replace,酒馆工具整合只 Add `workbenchDefs`。方向与既往 ITab_Bills 红字问题一致,属上游收敛。
2. **`HSK_FishTrap` 的 `CompProperties_Refuelable`:`fuelConsumptionPerTickInRain 0.01` → `externalTicking true`**(Building_FishingPier.xml)—— 改用 1.6 外部 tick 机制,符合性能铁律方向。注意:该 comp 不再有雨天油耗字段,雨天 fishing pier 行为由上游新逻辑决定。
3. 两个 DLL 均为上游重编译:同尺寸、87% 字节差异、无本地注入痕迹(`NoMaxBills`/`HSKFix`/`ilspy` 计数 0),即部署端此前也未被手工改过。

## 3. 本地兼容核对

- **`HSK修复整合` 的 Prepatcher `SKFacilityCrashFix.HSKVEFCompat.dll`**:运行时改 `Core_SK.dll` 里的 `"VanillaStorytellersExpanded.StorytellerWatcher:ExposeData"` 串。新 dll 内该串(UTF-16)仍命中 1 次,与旧版相同 ⇒ 补丁仍生效,上游未自行修复,勿删该预补丁。
- **CE 中文**:部署端 990 个 zh 文件中**唯一键 0 个**,故覆盖不丢本地翻译。此前统计的"31 处值不同"是假差异(本地 `-&gt;` 转义 vs 上游 `->`、本地残留英文 `<li>` 行),上游版已修正。
- About.xml 逐行核对无实质变化 ⇒ CE 的 `loadAfter`/`modDependencies` 未被回退。
- 工作区不涉及:这 8 个都是上游 mod,只落部署目录(沿用 08-25 起约定),无"双目录同步"事项。

## 4. 备份与回滚

更新前原文件:`_tmp\backup_上游更新_20260828\Hardcore-SK-20260828差分_部署前\<mod>\<相对路径>`
(CE 2025 + Core_SK 3 + Vile 1,共 2029 份;CE 有 1 个新增文件部署端原本没有,无备份)。回滚 = 按原相对路径 copy 回部署目录。
脚本:`_tmp/hsk_diff_20260828.py`(比对)、`hsk_ce_norm_check.py` / `hsk_ce_keydiff.py`(行尾与键级归因)、`hsk_apply_20260828.py` / `hsk_apply_ce_20260828.py`(逐文件原子替换 `.newtmp`+`os.replace`+md5 断言)、`hsk_verify_20260828.py`(终态核对)。

## 5. 重启事项

1. 含 `Core_SK.dll`、`CombatExtended.dll` ⇒ 必须完全退出再启动。当前 `RimWorldWin64`(PID 2252,自 08-27 20:54)仍是旧会话,本次仅落盘。
2. `MissileGirl\Cache\` 已在 09:07 被轮换为 `Cache_bak_20260828_090739`,当前不存在;老会话若在退出时回写缓存,启动前先确认 `Cache\` 为空/再次轮换,再取新的 `Unified.xml` 核对终态。
3. 旧存档无需重建:两处语义变更都是建筑 def 属性,读取即时生效。
