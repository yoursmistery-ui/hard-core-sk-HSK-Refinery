# 会话归档 · 外政/种族整合/修复交付（截至 2026-09-08）

> 用途：本会话（微信 IM 主会话）收尾交接。新会话读本文即可接续，不必翻聊天记录。
> 归档时间：2026-09-08。全量 Mods 备份同步产出中，路径见 §4。

## 1. 已完成并落盘（工作区 ↔ 部署双侧已同步）

| 主题 | 结果 | 位置 |
|---|---|---|
| 石化线 v3 彻底化 | 17 死 def + 6 物品 + 8 配方 + 3 研究清零；68 死贴图归档；`CompWasteAccumulator` 完整实现；GasCylinder 死路退役；About 重写 v3 终态 | `HSK工业科研大修/`，清单见 `docs/03_生产框架/石化线精简方案_v3_清除清单.md` |
| Player.log 冷启动修复 | 本地真错误 5→0（4 轮闭环）；含盔甲架 `uiIconPath` 错位致 3 def 整块加载失败、金鼠族篝火非法字段、22 个去木板打空、储罐缺 tickerType | `docs/07_排查记录/Player.log冷启动排查_石化线v3_20260829.md` |
| ProcessorFramework.dll 回退 | 05:17 Cecil 改坏 `Verse.Thing::def` MemberRef 签名 → 开小人面板每帧 OnGUI 异常（表现为"小人卡住"）；已回退干净版 | 双侧 `HSK工业科研大修/1.6/Assemblies/` |
| 鼠族壁炉火焰 | `fireSize 1.4→0.9` + offset 修正（火焰垂出炉底）；通行性误诊改动已回退 | `鼠族家具拓展/Defs/RKFC_JoyTemp.xml` |
| 美狐科技树融入 | 23 节点从沉底 NodesOther 融入主网格 4 个 HSK 页签 + 挂 HSK 前置；研究台六档补齐（LabStation 当研究台的硬伤已修） | `docs/01_科研体系/美狐融入HSK科技树归属表.md` |
| 特性拓展 v2/v3 | 动态授予引擎重建（一计数器一因果，修"熬夜越多反得早起鸟"）+ 全套文案层 + 加载顺位后移 | `特性拓展modHSK/设计/` |
| 全量漏译补译 | 玩家可见漏译 699→13（余为有意保留英文）；金鼠族 941 键错位、美狐 LoadFolders 缺 `/` 两处结构性真凶 | `docs/07_排查记录/全量漏译排查与补译_20260827.md` |
| 库林战狐整合 | 本地化 + 科技三段错位（入口挂石化中段 `RK_Petro_Refining` → 链尾顶 `CataphractArmor`）+ 派系权重降档；阿丽莎并存不动 | `docs/06_种族整合/库林Kurin_HSK整合方案.md` |
| 外政口径改向 | **自建 edgepolitics 容器已作废删除**（`_tmp/removed_边缘外政HSK_20260902/`）；改为复用现有 mod + 好感度机制（1.6 `GoodwillSituationDef` / `InitAllyTo` / `AlwaysAlly`） | `docs/10_外政与任务大修/`（9 篇） |
| 派系战争×政治边界 | Harmony 桥已编译进 `HSKFixPack.dll` 并双目录同步（`FactionalWarBordersLink.cs`） | `docs/10_外政与任务大修/派系战争x政治边界联动.md` |
| 性能优化 | 世界层与 BOTR 优化 | `docs/07_排查记录/性能优化_世界层与BOTR_20260905.md` |
| 四人扑克（Godot） | M1~M4 完成，113 单测全绿；Windows `poke.exe` + Android `poke.apk`（arm64 debug） | `C:\Personal\Project\poke\build\`，Godot 在 `C:\Personal\tools\` |

## 2. 未完成 / 待办（重要）

1. ~~**库林刺杀路由静默失效（未修）**~~ **✅ 已闭环（09-08）**：`任务大修HSK/1.6/Source/QuestBoardHSK/BountyRules.cs:257` 的 `GuildDefNames` 与 `HarmonyPatches.cs:354` 的 `MedievalGuildDefName` 已改指合并后的 `Kurin_Faction`；隐藏钉补遍历加 `def.hidden` 门控（Kurin 保持可见定居/族色发单方）；中/韩 `Factions_Kurin.xml` 清除孤儿 `Kurin_Faction_Hostile` 键；`QuestBoardHSK.dll` 重编（新 md5、字符串表 0 残留）＋双目录 hash 同步；冷启动核验无新增真错误。详见 `docs/10_外政与任务大修/刺客派系双阶整合.md`。
2. **金鼠族开局剧本未适配 HSK（进行中）**：开局科技不足，任务 `mtsnyujhp5nsa0hg` 处理中。
3. **真机冷启动验证欠账**：外政/库林/刺客/金鼠族等多轮改动均只做了静态验证，按约定冷启动由用户统一排期。注意 **Gagarin 缓存坑**：mod 列表变动会删 `MissileGirl\Cache` 而 `Dump()` 不重建目录 → `DirectoryNotFoundException` → **RimWorld 清空整个 mod 列表**；启动前**预建 Cache 目录**可规避。
4. 扑克后续：万能牌声明弹窗、结算比例公式（待用户给公式）、飞牌/翻牌动画、x86_64 APK。
5. 文档服务器（手机看文档）：`hsk_docs_server.py` 在主会话工作区，端口 8088，短链 `/wa` `/wb` `/d/N`；进程随会话结束会掉线，新会话需要时重启。

## 3. 环境要点（新会话必读）

- 工作区 `C:\Personal\Project\ratkin-patch`（**是 git 仓库**，改动可回退，免手工备份）；部署 `C:\Program Files (x86)\Steam\steamapps\common\RimWorld\Mods`。
- 双目录同步定位部署目录**必须用 About.xml 的 `<packageId>` 精确匹配**（子串匹配会命中别 mod 的 loadAfter 写错目录）。
- Mods 目录覆盖写入用 `.sync_tmp` + `os.replace`；`rm`/`del` 禁用，删除走回收站或 `_tmp`。
- 网络问题一律走本机 Clash Verge 代理 `http://127.0.0.1:7897`（curl `-x`；sdkmanager `--proxy`；gradle `~/.gradle/gradle.properties`）。备用镜像 `https://gh-proxy.com/`。
- 游戏进程可在用户授权下强杀做闭环验证（用户已给过完整控制权）；但**多任务并发时先协调归属**，别抢 Player.log。
- `Unified.xml` 不作唯一基准（缓存可能过期、且不含翻译注入后状态、不含运行时 implied def）。
- 用户口语别名：**魅狐 = 库林战狐 Kurin**（汉化用"魅狐×"前缀）；**绮罗 = Kiiro**（`绮罗HSK`，现已装）。
- 微信 IM 单文件上限 20MB，中文 URL 会被截断（发链接用 ASCII 短链）。

## 4. 交付物路径

- 全量备份（本次新出）：`outputs\RimWorld_Mods_full_<时间戳>.7z`（完成后复制 Downloads）
- 上一版全量：`Downloads\RimWorld_Mods_v3_20260829_2155.7z` + 900MB 分卷（**已过期**，仅存档用）
- 增量修复包：`Downloads\HSK_FixPack_20260829.zip`（PF 回退 + 壁炉，双击 bat 应用）
- 扑克成品：`poke\build\windows\poke.exe`、`poke\build\android\poke.apk`（超 20MB，分卷在 outputs）
- 文档：全部在 `docs/`，索引见 `docs/00_HSK参考文档合集导航.md`
