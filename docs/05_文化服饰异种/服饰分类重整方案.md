# 服饰分类重整方案 v1

> 状态: **已实施（2026-09-09）· 静态验证全绿 · 待游戏内验证**。§7 四点按本文建议定案（战斗护甲不细分 / 科技档保留 6 档且"远古"改名 `Archotech 终极` / 种族桶除 `Meow_*` 外归档 / `Set*` 中文名一并补齐）。
> **实施落地清单、对方案的三处改判、验证结果与复算命令 → `服饰分类重整_实施记录.md`**。数据源 `Mods/Unified.xml`(2026-09-09 09:29 终态)；脚本 `_tmp/apparel_cat/`。
> 附表: `服饰分类重整_三轴归属表.csv`(910 件逐件三轴归属+落地方式)、`服饰分类重整_映射表.csv`(115 个旧分类处置)。
> 截图那棵树 = Allow Tool 过滤器，渲染 `ThingCategoryDef` 树 + `SpecialThingFilterDef` 伪节点。

## §0 现状诊断

910 件服饰，`Apparel` 树下 **116 个分类节点，31 个平级挤在一级**。根因是**五套互不相容的分类轴被当成同级兄弟**：

| 轴 | 代表节点 |
|---|---|
| A 穿戴层(Core_SK) | UpperDressCat 衣物上衣 / PantsCat 下衣 / VestsCat 背心 / BodySuitsCat / FullArmorCat / Headgear / BootsCat / GlovesCat |
| B 穿戴层(Vile's Pre-Industrial，**与 A 平行重叠**) | BaseLayerCat 内衣 / MiddleLayerCat / ShellLayerCat / CombinedArmorCat |
| C 用途性质 | ApparelArmor 装甲衣物 / ApparelNoble 贵族 / ApparelUtility 辅助 / ApparelMisc 杂项 / SpecialApparelCat 特殊装备 / ShieldCat 盾牌 / AccessoriesCat 配件 / KidsApparel |
| D 种族来源 | `RK_Apparel`(**225 件**)、`Miho_Apparel`(56)、`Kurin_Apparel`(64)、`OARatkin_*`、`Meow_*`、`RK_Scroll` |
| E 身份套装 | `Sets`(截图"按用途分类") + 33 子 |

病灶：**263 件(29%)有 ≥2 分类**，几乎全是 `Set*` 重复挂一遍（`Apparel_TunicPoor` → MiddleLayerCat+SetMedieval+SetCommoner+SetSlaver+SetHarvester）。**标签撞车**（`headgear`×2、`shields`×3、`Full Armor`×3、`Heavy/Light/Medium Armor` 各 2~3 份挂在三个不同父节点下）；**13 个空节点**（Hazmat/Mopp/Loadout/Sets/PreModernSets/SetJet/Shields/VFEC_Shields/UpperDressCat/HeavyUpperArmorCat/Kurin_Apparel/Kurin_Apparel_Middle/RK_HeadgearNoble）；`Miho_Apparel` 的 label 还是韩文 `미호 의상`。

底层数据质量：`Backpack`(12) 与 `BackPack`(8) 两套大小写；6 件 `layers` 内重复（`Belt/Belt`、`Overhead/Overhead`、`Waist/Waist`）；`Apparel_HeavyShield` 无任何分类。⚠ **`Overhead` 是头顶层（帽/盔），不是外层大衣**——按字面理解会整批判错。

## §1 引用安全边界（已核，决定能动什么）

- 全 236 个 mod 的 **DLL 零硬引用** `Set*`、种族桶、`*LayerCat` 的 defName（扫描器用 `Apparel`/`CombatExtended` 做对照验证）。分类消费全走 XML 数据驱动。
- 唯二 XML 硬引用，**必须保命**：① `SetChemicalProtectionSuit` → `Core_SK\Defs\TraderKindDefs\TraderKinds_Orbital_DeepSpaceMining.xml` 的 `StockGenerator_Category.categoryDef`（深空采矿船库存）；② 7 个层分类（Base/Middle/Shell/Combined Layer + Middle/ShellFull/ShellPartial Armor）→ `HMC Vile's Pre-Industrial\Patches\Vile_Mending.xml` 的 `ingredients.filter.categories` + `fixedIngredientFilter`（修甲配方收料）。
- ∴ **只改 `parent`/`label`/成员挂载，绝不删 def、绝不改 defName**（改 parent 不影响成员关系，上面两处都安全）。
- `<apparelTags>`/`<tags>` 里的 `RK_Apparel`、`OARatkin_Apparel`、`Meow_*` 等**同名字符串属另一命名空间**（`PawnApparelGenerator` 只做字符串比对），是文化穿搭池标签，**勿动**。
- 截图顶部三个 `*技术等级` 不是分类，是 Core_SK 的 6 个 `SpecialThingFilterDef`（`AllowApparelNeolithic…Ultra/Archotech`）。该类型**无 displayOrder 字段**，顺序由 def 加载序决定、XML 不可排 → 保持原样，不并入新树。⚠ `ThingCategoryDef` **没有任何排序字段**（1.6 反编译确认），子分类顺序由 def 加载序决定、XML 指定不了。首版误写 `displayOrderInCategory` 炸了 27 条 `doesn't correspond to any field`，已全撤（详见实施记录）。

## §2 目标树（三轴必挂 + Sets 保留）

每件服饰**同时挂满 ①②③ 三个分类**，`Set*` 成员关系原样保留；旧分类不删，**降级为对应新节点的子节点**，一级只剩 4 个。

```
服饰 Apparel
├─ ① 按穿戴位置 HSK_P_Root (9)   躯干外衣300 / 头部279 / 配饰腰背115 / 贴身内衣85 /
│   │                            全身连体56 / 面饰眼部23 / 下衣裤裙18 / 手套18 / 鞋靴16
│   └─ 旧 A/B 轴分类 re-parent 进来（BaseLayerCat→贴身、Shell/Middle→躯干、Headgear 系→头部…）
├─  按用途场景 HSK_U_Root (7)   战斗护甲427 / 日常便服264 / 礼仪贵族80 / 职业劳作46 /
│   │                            装饰饰品39 / 战术装备38 / 环境防护16
│   └─ 旧 C 轴分类 re-parent 进来（ApparelNoble→礼仪、Utility/Misc/Special→战术、ShieldCat→战斗…）
├─  按科技档位 HSK_T_Root (6)   中世纪466 / 工业247 / 太空131 / 原始53 / 极致10 / 远古2
├─ ④ 按身份套装 Sets (33)        原样保留 + 补中文名 + displayOrder 排末尾
└─    种族桶 RK_Apparel/Miho_Apparel/Kurin_*/OARatkin_* → 成员拆入①②后停用
                                    （例外: Meow_* 首饰保留，re-parent 为 ①配饰 的子节点）
```

## §2.1 复用清单

实测 22 个目标节点里 13 个可复用现成 def、只需新建 9 个。⚠ **实施时改判为「轴节点一律新建、现成 def 降级为其子分类」**（避免与既有成员撞 `duplicate thingCategory`），故实际新建 26 个。
逐节点成员依据、原版/Core_SK/Vile PI 归属判定、`BaseLayerCat` 被 Vile 修甲配方引用的处置注意 → `服饰分类重整_复用清单.md`；改判理由与落地清单 → `服饰分类重整_实施记录.md`。

## §3 三轴判定规则（已跑通全量，可复算）

- **③ 科技档**：直取 `techLevel`，零歧义。HSK 的"电力/石化"是科研树概念、不是 def 字段，browse 层用 techLevel 六值。
- **① 穿戴位置**：优先级 `旧功能分类 > Zone_* tag > layers > bodyPartGroups`。三信号一致 635/910(70%)，冲突 275 件集中在"OnSkin 层的制服/常服"(109) 与"甲类算全身还是躯干"(29)——属语义选择而非数据错误，按优先级定档并全量落表可复核。**① 的 9 类直接复用既有 `Zone_*` 8 分区 tag**（见 `文化服饰偏好系统方案.md`），让浏览树与文化穿搭共用一套口径，不再造第四套。
- **② 用途场景**：旧分类映射 → 甲类分类或护甲值≥4 判战斗护甲 → `Set*` 职业/战斗归属 → 按 techLevel 兜底日常便服。⚠ 薄弱点: **55 件靠"护甲值≥1"兜底判成战斗护甲**（如 airwire headset 通讯耳机），P2 需抽检并收紧阈值。另 22 件（轨道轰炸指示器/心灵投射枪/控制包/连体工作服/热力大衣）已人工裁决，清单在 `_tmp/apparel_cat/finalize.py` 的 `MANUAL`。
- 结果: **910 件全部拿到三轴归属，0 件待裁决**。

## §4 落地拆分

| 落位 | 内容 | 位置 |
|---|---|---|
| 新分类 def | 原估 9 个，实施改为 26 个（轴节点一律新建，理由见 §2.1 改判）；label 直写中文，不写 `displayOrderInCategory`（该类型无排序字段） | `HSK修复整合/Defs/ThingCategoryDefs/服饰三轴分类.xml` |
| 补丁 | 115 个旧分类 `Replace parent` 收拢 + 476 件上游服饰补挂三轴，全部顶层独立 `PatchOperationConditional` 门控 | `HSK修复整合/Patches/58_服饰分类三轴统一.xml`（58~99 空档；**严禁 1xx**） |
| 本地直改 | 434 件: 鼠族HSK拓展 221 / 金鼠族 77 / 美狐 71 / 库林 65 —— 直接改各 mod Defs 的 `thingCategories` | 工作区对应 mod，双目录同步 |
| 汉化 | 新节点英文名 + `Miho_Apparel` 等旧 label 补译 | `HSK修复整合/Languages/ChineseSimplified (简体中文)/DefInjected/ThingCategoryDef/`（根节点必须 `<LanguageData>`） |

**写法三条硬约束**

1. **模板照抄 `52_弹药分类统一.xml`**——它已验证过同一套动作（新建中类 + 顶层 Conditional 门控 + `Replace parent` 收拢），弹药分类树就是这么收编的。
2. PatchOperation **不解析继承**: 只补原始 XML 里自带 `thingCategories` 的 def；靠基类继承分类的，须打到基类 `[Name="XXXBase"]` 或在子类自身 Add 完整列表。
3. **父子 def 不得同时补同一分类**（AGENTS §3，会撞 `duplicate thingCategory`）——补之前在原始 Defs 上按 ParentName 链排查；两分支 value 一律带 `Inherit="False"` 挡父类旧列表项。

## §5 分期

P1 骨架(9 新 def + 13 处复用改 label/parent + displayOrder) → P2 挂载 910 件(先 434 本地，再 476 补丁；抽检 55 件护甲值兜底区) → P3 降级(115 旧分类 re-parent、`Sets` 补中文名排末尾) → P4 清理(种族桶停用、13 空节点归并、撞名消除、`BackPack`/同层重复修正) → P5 验证同步。

## §6 验证（不启动游戏）

`_tmp/patch_simulator.py`(lxml) 模拟 0 failed → 重跑 `_tmp/apparel_cat/scan.py` 断言（一级节点 ≤5、每件 ≥3 分类、0 悬空、除 Sets 容器外 0 空节点、同层 label 唯一）→ `crosscheck.py` 看分布漂移 → 双目录逐文件 hash + About/HSK 标记核对。

## §7 待拍板

1. **战斗护甲 427 件**是否再按 轻/中/重/动力/防化 细分？建议**不细分**，交给降级后的旧甲类做二级。
2. **科技档** 极致 10 / 远古 2 是否并入太空缩成 4 档？建议**保留 6 档**，但"远古"改名 `Archotech 终极`以免与"原始"混。
3. **种族桶**停用隐藏，还是 re-parent 当子分类？建议 `Meow_*`(首饰) 保留为子分类，其余 `RK_/Miho_/Kurin_/OARatkin_` 停用。
4. **`Set*` 33 项中文名**（现全英文如 `Archer Set`/`Norbal Set`）是否本次一并补？建议**一并补**。
