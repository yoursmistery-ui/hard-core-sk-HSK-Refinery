# 食物分类重整方案 v1

> 状态: **方案·未实现，待 §8 拍板**。数据源 `Mods/Unified.xml`(2026-09-09 终态)；脚本 `_tmp/food_cat/`（scan_food/probe/probe2 三个脚本）。
> 附表: `食物分类重整_剪枝判定表.csv`（37 分类逐个判定）。
> 姊妹篇: `服饰分类重整方案.md`/`_实施记录.md`（同套路工具）；食物域约束严苛得多，差异见 §1。

## §0 现状诊断

485 件可食用物品（带 `<ingestible>`），散落在 **6 个互不相邻的顶层根**里：

| 所在根 | 件数 | 说明 |
|---|---|---|
| `Foods` | 265 | 真食物（餐/生食/饮品/保存品/饲料…） |
| `Plants` | 152 | 植物本体（放牧/树木），父=Root |
| `MedicineStuff` | 46 | 药品与功能剂（已被 CoreModule 挪到 `Manufactured` 下） |
| `BodyParts` | 11 | 可食用器官（人肉/虫肉体系） |
| `ResourcesRaw` | 8 | 植物材料/提取物 |
| （无分类） | 6 | Glowstool、Agarilux、Bryolux、Brightbells、NewYearTree、Plant_MotherAmbrosiaLGE |

`Foods` 内部结构失衡：一级 11 个，但 `FoodRaw` 自身只有 1 件直接成员却充当 120 件的父节点；`MeatRaw`(4 件) 下面挂着 `Fish`(14 件)；`Alcohol`(0 件) → `Alcohol_Bottles`(16) + `Alcohol_Crates`(15) 三层套；`Foods` 根节点上直接挂了 `DBH_WaterBottle`。另有 **21 件双挂**（`Pemmican`→FoodMeals+Preserves、`RawFungus`→PlantFoodRaw+MeatSubRaw 等）、**4 个空节点**（`OtherEdible`/`FoodFreshFish`/`Crops`/`Trees`）、**撞名**（父 `Alcohol` 与子 `Alcohol_Bottles` 的 label 都叫 Alcohol）。

## §1 引用安全边界 —— 与服饰最关键的差异

服饰那边"改 parent 安全"的结论**在食物这边不成立**。`ThingFilter.SetAllow(分类)` 与商人库存取的是该分类的**整棵后代**（`DescendantThingDefs`），而食物域大量引用的恰恰是**祖先级**分类：把 `MeatRaw` 从 `FoodRaw` 挪走，`FoodRaw.DescendantThingDefs` 立刻少 18 件 → 所有以 `FoodRaw` 收料的烹饪配方当场少一类原料、卖生食的商人当场少一批货。

实测承重（37 个分类里 **32 个承重**，逐条引用方见 `食物分类重整_剪枝判定表.csv`）：配方收料（`Foods`/`FoodRaw`/`MeatRaw`/`PlantFoodRaw`/`AnimalProductRaw`/`Eggs*`/`Fish`/`CookingSupplies`/`LuxuryStuffs`/`SweetMeals`/`Energetics`/`Alcohol_Bottles`/`Drugs`/`Medicine` 等，引用方含原版 Data、Core_SK、Vile's Amuse Bouche、金鼠族、美狐、鼠族HSK、任务大修、Common Sense、本仓修复整合）；商人 `categoryDef`（含空节点 `FoodFreshFish`）；建筑存储 `thingFilter`（Core_SK Buildings_Storage 一处就引用几乎全部，含 `disallowedCategories`）；4 条 Ideology 素食/肉食/同类相食/虫肉 `SpecialThingFilterDef` 的 `parentCategory` 直接挂在 `Foods` 上；代码级 vanilla DefOf 绑 `FoodRaw/MeatRaw/Fish/PlantFoodRaw/Eggs*/Crops/Stumps/Foods/Plants/Drugs/Medicine/OtherEdible`、Core_SK `SkyfallerUtil` 比 `FirstThingCategory != MeatRaw`、Animal Controls 运行时动态归类 `Trees`/`Crops`。
- ⚠ **两条顺序铁律**：`Stack XXL` 只看 `thingCategories[0]`、Core_SK 比 `FirstThingCategory` → 新分类**必须追加在列表末尾**，插到第一位会静默改变这些判定。

∴ 硬约束：**只新增分类 def + 只往 `thingCategories` 末尾追加；现有 37 个分类的 `parent`、`defName`、成员一个都不动。**

## §2 剪枝可行性：只有 5 个能剪

37 个食物域分类逐个判定后，**可剪 5 个 / 承重保留 32 个**。可剪的是 `OARatkin_FoodMeals`(8 件)、`Spoilage`(2)、`MedicalDrugs`(12)、`MedicalMaterials`(1)、`Serums`(0) —— 只出现在成员列表、无任何过滤器/商人/代码引用。4 个空节点（`OtherEdible`/`FoodFreshFish`/`Crops`/`Trees`）**不能删**：分别被 Animal Controls 的 DefOf 和 Core_SK 商人的 `categoryDef` 硬引用。

⇒ 食物域的"剪枝"只能是**展示层收纳**：把 32 个承重分类作为三轴视图的**二级子节点**挂进去（改的是"谁显示在谁下面"这件事的可见性，而不是它们的 parent——因为 parent 一动就改后代集合）。具体做法见 §3：**新轴叶子各自独立挂物品，旧分类原样留在原位当业务分类**，两套并存，靠命名与顺序让浏览清晰。

## §3 目标结构（保留原树 + 叠加三轴）

```
Foods(原样不动) ├─ FoodMeals/FoodRaw/CookingSupplies/... 32 个承重分类原样
Plants / MedicineStuff / BodyParts / ResourcesRaw  原样不动

新增 3 个视图根(挂 Apparel 的对应位置 = Root 下, 与 Foods 平级? 见下)
├─ ① 按食用场合  HSK_F_Use   (7)   奢华餐21 / 精致餐37 / 简餐30 / 粗劣餐9 /
│                                   生食212 / 应急与饲料95 / 非营养功能件95
├─ ② 按来源      HSK_F_Src   (8)   植物性177 / 成品餐88 / 蛋奶动物产品69 /
│                                   加工食品44 / 树木本体39 / 肉类31 / 饮品27 / 真菌种子9
└─  按科技档位  HSK_F_Tech  (7)   HSK 六档(原始/中世纪/电力/石化/太空/极致) + 未分档
```

三根轴各自独立成 `Root` 下的顶层根（与 `Foods`/`Plants` 平级），**不挂在 `Foods` 下面** —— 既不改 `Foods.DescendantThingDefs`（商人/存储/配方零影响），也避开 §7 的显示根劫持。代价是树顶层多 3 个根。

## §4 三轴推导规则与实测覆盖率

- **① 食用场合**：直取原生 `ingestible/preferability`，485 件 100% 覆盖。`MealLavish/Fine/Simple/Awful·Terrible`→四档成品餐；`RawTasty/RawBad`→生食；`DesperateOnly*`→应急与饲料；`NeverForNutrition`→非营养功能件。
- **② 来源**：直取原生 `foodType`（逗号串取首个命中项），100% 覆盖。`Plant/VegetableOrFruit`→植物性、`Meal`→成品餐、`AnimalProduct`→蛋奶、`Processed`→加工、`Tree`→树木本体、`Meat`→肉、`Fluid`→饮品、`Fungus/Seed`→真菌种子、`Kibble`→饲料。
- **③ 科技档**：vanilla `techLevel` 只有 **26/485** 自带，改用 HSK 科研节点档位（`docs/01_科研体系/科研节点总览.md` 的「档位」列，545 条）四级推导：产出配方研究节点 **140** → 产出工作台解锁档 **14** → 最早消耗配方研究档 **58** → 自带 **26** ⇒ **238/485 = 49%**。剩余 247 件几乎全是**天然产物**（蛋 62、植物本体、奶、生肉），无产出也无消耗配方；补两条默认规则`天然动物产物/野生植物 → 原始档`、`无来源装饰/功能件 → 未分档桶`，预计未分档收敛到 30 件以内，**逐件列附表复核**。
- ⚠ 解析坑（已踩）：`Unified.xml` 把 `RecipeDef.products` 序列化成简写 `<products><MealSimple>1</MealSimple></products>`（元素名即 defName），按 `<li><thingDef>` 解析会漏掉全部产出配方、把覆盖率误报成 11%。

## §5 顺手修（四项全做）

① 补 6 件无分类（纯追加，安全）。② 撞名与层级纠偏：只改 DefInjected label（`Alcohol`→酒类（箱）、`Alcohol_Bottles`→瓶装酒、`FoodRaw`→生鲜原料…），不动结构。③ **21 件双挂不拆** —— 它们是配方/商人在用的（`Preserves` 与 `FoodMeals`/`MeatRaw` 交叉），拆开即改行为；改为在①②轴里给唯一归属，旧双挂原样保留。④ 4 个空节点保留 def，label 标注用途（如 `FoodFreshFish`→鲜鱼（商人专用池））。

## §6 落位与分期

| 落位 | 内容 |
|---|---|
| `HSK修复整合/Defs/ThingCategoryDefs/食物三轴分类.xml` | 3 视图根 + ①7 + ②8 + ③7 = **25 个新 def**，label 直写中文；**不写 `displayOrderInCategory`**（`ThingCategoryDef` 无排序字段，服饰那边已踩过） |
| `HSK修复整合/Patches/61_食物三轴挂载.xml` | 上游/原版可食物品补挂三轴（Conditional：有 `thingCategories` 则末尾追加 `<li>`，无则整体 Add）；编号避开 58/59 与并行会话占用的 60 |
| 金鼠族 / 美狐 / 鼠族HSK / 工业大修 各自 `Patches/` | 本 mod 自有食物补挂（金鼠族 `OARatkin_FoodMeals` 那 8 件等） |
| `HSK修复整合/Languages/.../DefInjected/ThingCategoryDef/食物三轴分类.xml` | 新节点名 + 撞名/空节点 label 纠偏（HSK修复整合 位 221 > 汉化包 194/195，DefInjected last-wins） |
| `HSK修复整合/Source/ApparelAxisRootFix.cs` | **把三根食物轴加进显示根回退名单**（见 §7） |

分期：P1 骨架 25 def → P2 ①②轴挂载（100% 原生字段，无推导）→ P3 ③轴推导 + 天然产物默认档 + 未分档清单复核 → P4 顺手修 4 项 → P5 静态验证 + 双目录同步。

## §7 必须同步处理：显示根劫持

`Verse.ThingFilter.RecalculateDisplayRootCategory` 会把「子树包含全部候选物品」的分类里**遍历序最后一个**当显示根，带 `parentFilter` 的窗口（配方原料选择那类）用它当树根 —— 服饰已踩过并用 DLL 回退修掉。三根食物轴各自覆盖全部 485 件，必然重演。本方案靠 §3 的「视图根与 Foods 平级」从结构上规避；同时仍把 `HSK_F_Use/HSK_F_Src/HSK_F_Tech` 加进 `ApparelAxisRootFix.AxisRoots` 兜底，回退目标按候选所属根选 `Foods`/`Plants`。上线前用 `_tmp/apparel_cat/reparent_impact.py` 同口径模拟验一遍显示根落点。

## §8 待拍板

1. **③科技档做不做**：严格推导只 49%，其余靠默认档 + 未分档桶。建议做，但 247 件默认档/未分档单独列附表复核。
2. **视图根挂哪**：与 `Foods` 平级成 3 个顶层根（推荐，零劫持风险、零行为影响），还是塞进 `Foods` 内部（紧凑但必须靠 DLL 兜劫持）。
3. **152 件植物本体 / 46 件药品**：范围既然是全部可食用，①②轴会覆盖它们（树木本体 39、非营养功能件 95）。确认真要在食物视图里看到，还是排除。
4. **5 个可剪分类**：剪 = 把成员从旧分类摘掉只留三轴。建议**不摘**，只在展示上并入①轴，def 与成员原样留着（摘了会牵动金鼠族商人库存）。
