# 科研坐标撞车:CSP_MechChargerUpgrade × Miho_CommandDrone(2026-09-03)

> 09-02 上游更新(见 `HSK更新记录_20260902.md`)后,冷启动报:
> `ResearchTreeSK :: 支援无人机(20,27) have the same position as Mech Charger Upgrade(20,27)`。

## 归因

- `Mech Charger Upgrade`(`CSP_MechChargerUpgrade`)= 本次更新从 `Core_SK_DLCModule/Biotech/Defs/Research/Research_MechUpgrades.xml` **新增**的上游节点,`tab=Buildings_SK`,坐标 (20,27)。
- `支援无人机`(`Miho_CommandDrone`,美狐 HSK 拓展,`1.6/Defs/ResearchProjectDefs/Research.xml`)= **本地**节点,同 `tab=Buildings_SK`,原本也在 (20,27)。
- 上游件下次更新会覆盖回原样,故**动本地节点**、不动上游。全树普查(`_tmp/census_research_pos.py`)确认仅此 1 个撞车格。

## 修法

`Miho_CommandDrone` 的 `researchViewX` 20 → **21**(Y 保持 27)→ 移到 (21,27)。

- 选 (21,27) 依据:普查中该格空;仍在无人机行(y27);满足"前置在严格左侧"——同 tab 前置 `Miho_LightWarDrone`(x16)、跨档 `HighMechtech`(x19)均 21>它们。
- `CommandDrone` 无任何**已落位研究节点**以其为前置(下游是 ThingDef/配方:`Races_Mechanoids_Heavy`、`Experimental/TransportRace`、`VEFM/MechanoidBuilding` 的 `researchPrerequisite`),故**不产生连锁位移**。

## 验证

- `ET.parse` 通过;工作区与部署 `美狐HSK拓展/1.6/.../Research.xml` md5 一致(`157516702f0be4…`)。
- 静态复核同文件其余 `x=21` 节点在 y28/y38,无新撞车;MechCharger 独享 (20,27)。
- `_tmp/gen_research_doc.py` 读的是游戏合并态 `Unified.xml`(仍是重启前 (20,27)),**暂不重跑**,由下次冷启动 Unified 刷新后再生总览,以免写回旧坐标。

## 生效

纯 XML 改动,需游戏完全退出后冷启动生效(与 09-02 那批 DLL 一并)。
