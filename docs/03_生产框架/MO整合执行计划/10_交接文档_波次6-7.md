# 交接文档 · MO 第二批波次 6/7（2026-08-29 03:30 更新）

> **给下一段对话的自己/接手者**。本批共 9 波，**波次 6 已完成**(2026-08-29 凌晨, 细节见 `11_波次6_KCSG布局落地.md`), **剩波次 7(任务链)**。
> 完整规划见 `09_第二批全量补齐计划.md`、进度看板 `../MO整合进度盘点_实测版.html`。

## 〇、波次 6 完成态(勿重做)

- 31 个 MO KCSG 布局已全部移植为 `RK_MO_*`(8 个布局文件) + 696 个 `RK_Sym_*` 符号, 在 HSK工业科研大修; 站点注入在 任务大修HSK `Sites_RK_Ruins.xml`(BanditCamp 6 布局随机池 + ItemStash 圣物库)。
- **顺手修了 P1 的 thingDef no-op bug**(VEF SymbolDef 字段是 `<thing>`), P1 废墟从"从未生成"变为正常生成。
- 锻铁砧摆件 `Rustic_Anvil` 落地(辅助建筑>乡村装饰); 烤架=TableGrill(用户拍板不用火盆); 全部 MO 兵种=KingdomBrigand(王国强盗); 怪物尸体=骨堆; 全部已双目录同步(md5 核对 16 文件)。
- 蛇窟/邪教/定居点/城堡/矮人堡垒 21 个布局**已建好未挂站点**, 波次 7 直接引用 `RK_MO_*` defName 即可。

## 一、当前状态（可查证，勿凭记忆）

- 工业大修 def 总数：**707+31 波次6**。新增：研究节点 1、照明 9、家具 36、废墟场景 43、冰块/冰柜/采冰 3、文化花样 9。
- **双目录零差异(2026-08-29 全量 diff 复核)**：工作区 ↔ `Mods\HSK工业科研大修` 与 `Mods\任务大修HSK` 逐文件 md5 一致、无嵌套目录。两处历史欠账已回灌：
  - `Languages\English\...\BlueprintBooks.xml`：部署目录有单行索引式重写版(258 条, 08-29 02:53, 并行会话产物)，以其为准回灌工作区；
  - 任务大修的 8 个悬赏/委托汉化文件(`SW_Warrants`/`SW_Sites`/`SW_Visitors`/`SW_WantedReasons` DefInjected + `RQ_Jobs`/`RQ_Keys`/`Keyed`)原本只在部署目录，已回灌工作区 `Languages\ChineseSimplified (简体中文)\`。
- 研究节点：`RK_PrimitiveLighting` 已建（Buildings_SK Y=9 X=2，全局跨 tab 空位验证过）。

## 二、DLL：✅ 已编译并部署(2026-08-29)

- build.ps1 编译通过(此前 csc 被策略拦截, 本次已放行), 产物 `1.6\Assemblies\BlueprintUnlockHSK.dll`(79KB)已 md5 核对同步到部署目录; Source/ 与 build.ps1 亦同步。
- 编译时修了最后一处错: `FurnitureMechHSK.cs` 的 `bed.CurOccupants` 在 1.6 是 `IEnumerable<Pawn>`(惰性枚举), 原 List 索引写法编不过, 已改 foreach。
- `CompIceChestHSK`(冰融化随室温)与 `CompFurBedCureHSK`(毛皮床治低温症)现已生效; 两边 Assemblies 均只含 BlueprintUnlockHSK.dll + ProcessorFramework.dll(捆绑 PF 框架本体), 无同名残留。

## 三、波次 6：31 个 KCSG 布局（最大块，方案未定，需用户拍板）

**分析结论**（脚本 `_tmp/analyze_kcsg_gap.py`，明细 `_tmp/kccg_symbol_gap.json`）：
- MO 31 个布局：**824 种符号 → 487 种 thing**
- **可直接映射 109 项**（Rustic_/RK_ 前缀命中——本批移植的家具/废墟/照明全部能用）
- **缺失 324 项**，四类：
  1. **MO 手动工作台/炊具 ~30+**：Anvil(铁砧)、Grill(烤架)、ButchersBlock(屠夫案板)、Grinder(磨盘)、Furnace(熔炉)、Bellows(风箱)、CleaningBench、DryingRack、ScribeTable(抄写台→我们有 RK_ScribeTable 但映射表没认)、AdvancedResearchBench 等
  2. **pawn 守卫**：Brigand/Cultist/Amboise/Hesse/Oswin/Soren 各兵种（用户已决策派系不做，但废墟需要守卫）
  3. **装饰小物/容器**：矿石桶、砖堆、Crate、Cart、Corpse_*（MO 怪物尸体，生物没做）
  4. **映射漏网**：CultOrb/CultPedestal/CultShelf → 实为 RK_AncientOrb/RK_OldPedestal/RK_ArchiveCabinet（映射表需补别名）
- 误报注意：Campfire/Carpet*/Column 等原版符号在 `Data/Core` 的 SymbolDef，不在 ThingDef，按 thing 判断会误报缺失。

**三条路线（用户 08-29 01:30 尚未拍板，新对话先问这个）**：
- A. **替代式移植（推荐）**：图纸照搬，缺失符号用我们的相近 def 顶替（Grill→Rustic_Brazier、Anvil→留空），守卫用原版 BanditThug；109 项真实映射 + 顶替表
- B. **完整补做**：把缺失的 30+ 个 MO 小物也移植（工作量大，很多是纯摆设）
- C. **精简版**：只做小/中废墟 + 蛇窟 3 套，藏身处 13 + 定居点 12 先搁置

**实现要点（无论哪条路）**：
- 符号定义用我们的格式：`<KCSG.SymbolDef><defName>RK_Sym_*</defName><thingDef>Rustic_*</thingDef><rotation>`（参考 `任务大修HSK/1.6/Defs/Sites_RK_Ruins.xml` 的 19 个 RK 符号）
- 布局文件放工业大修 `1.6/Defs/StructureLayouts_RK.xml`（或拆文件），GenStepDef.linkWithSite 注入原版 BanditCamp/ItemStash 的方式沿用 P1（参考任务大修HSK 的 Sites_RK_Ruins.xml）
- **MO 符号名编码参数**（`{DefName}_{Stuff}_{Rotation}`），转换时按符号名拆解 → 映射 thing/stuff/rotation
- 布局里的地形符号（Granite/Wall_BlocksGranite 等）多数原版在场，直接用
- 涉及 VEF-KCSG：工业大修 About 已声明 VEF-KCSG 依赖（VanillaExpandedFrameworkLocal）

## 四、波次 7：任务链（加入「任务大修HSK」）

- 邪教三级收集链 + 蛇窟清剿，用户拍板**放进任务大修HSK**（packageId `local.hsk.questoverhaul`）
- ⚠ 需要**自建 QuestScriptDef / SitePartDef**——推翻 P1 当时"不自建、注入原版站点"的省事方案；意味着要写任务脚本
- 依赖波次 6 的废墟布局（藏身处/蛇窟）作为任务目标
- 参考：任务大修HSK 现有 QuestScriptDefs 的写法（`Script_Warrant*.xml` 等 15 个族色委托脚本）

## 五、复用工具（都在 `_tmp/`）

| 工具 | 用途 |
|---|---|
| `scan_research_coords.py [tab] [y_lo] [y_hi]` | 研究坐标实时扫描 + 跨 tab 冲突检测（**别信 Mods/Unified.xml**，那是旧快照） |
| `verify_mo_defs.py <Defs相对路径>` | XML / 贴图（单张+目录+方向）/ 材料与研究引用 / 菜单一致性四合一校验 |
| `scan_mo_landed.py` | def 全量采集（**必须用 ET 遍历，正则抓 defName 会漏 90%**） |
| `gen_rustic_furniture.py` | 数据表驱动生成家具 def（改数据重跑） |
| `gen_rustic_ruins.py` | 生成废墟场景物件 |
| `analyze_kcsg_gap.py` | KCSG 符号缺口分析（波次 6 专用） |
| `sync_batch2.py` / `sync_ruins.py` | 双目录同步模板（逐文件 hash 核对） |

## 六、写代码/XML 前必读铁律（本批踩过的坑）

1. **C#5 兼容铁律**：build.ps1 用 Framework64 csc（C#5）。禁止 `$""` 插值（用 string.Format）、`=>` 表达式体（用 get{}）、`?.`（显式 if）、`out var`、局部函数。FurnitureMechHSK.cs 头部有完整声明。
2. **`CompProperties` 没有 `ConfigErrors()`**（那是 Def/DefModExtension 的），别 override，配置校验改运行时宽容。
3. **冰柜机制已定型**：ASF 体系（AdaptiveStorageBase + `<temperature><requiresFuel>true</requiresFuel>`），**制冷/开关全靠 ASF 原生**，自研 comp 只做"消耗速率随室温"。别改回 HeatPusher 方案。
4. **工业大修 About 已加 ASF 依赖**（adaptive.storage.framework，modDependencies + loadAfter）——新增引用 ASF 的 def 前先确认。
5. **贴图**：MO 贴图在根 `Textures/`（不在 1.6/）；`Graphic_Random` 是目录变体要递归复制；缺 `_west` 不用补（游戏自动镜像，项目 114 个贴图集都这样）；stuffable 可建造建筑必须写 `uiIconPath`（`{tex}_south` 或 `_north`），场景物件（不可建造）不用。
6. **Python 改 XML**：只做目标字符串替换；改完查 `\r\r` + ET.parse + 双目录 md5。
7. **研究坐标**：新建节点用 `scan_research_coords.py` 查全局空位；`Mods/Unified.xml` 是 08-28 旧快照（缺 4 个新节点、仍含已删的 Rustic_Kiln），不可信。

## 七、备忘

- `Rustic_Kiln`（砖窑）与 `Rustic_Smoker`（烟熏房）：用户确认**有意删除不移植**，About 已改写，勿恢复。
- 生产辅助链接件：用户拍板**不做**（抄写 4 件保留）。
- 专著：VBE 12 本已全齐且汉化，**无需任何工作**（只补了 jobString 汉化）。
- 波次 6 路线（A/B/C）待用户拍板——新对话第一件事就是问这个。
