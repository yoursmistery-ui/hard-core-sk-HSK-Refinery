# 任务大修HSK · 悬赏故事化七/八期详录(2026-08-31)

- **⚠ 关键修复(先于一切)**:`ModEntry.cs` 新增——此前**整个程序集没有 PatchAll 引导**,六期全部 HarmonyPatch 从未生效、`BountyRadioManager` 从未注册(GameComponent 无处挂)。现 `[StaticConstructorOnStartup]` 统一 `new Harmony("QuestBoardHSK.ratkin").PatchAll()`;GameComponent 用 `Game.FinalizeInit` postfix `components.Add`(1.6 无 `InsertComponent` API)。**教训:新 DLL 光有 `[HarmonyPatch]` 特性不会生效,必须有引导类。**
- **发单成功**(`MakeWarrantDialog` prefix-false 重写,`BountyTales.cs`):故事版付款弹窗——五变体文案(`Tale_IssueSuccess_0..4`,八期扩 3→5,占位 THING/FACTION/TARGETFAC/CRIME/REWARD)+ 生死附加句(`Tale_IssueOutLiving/OutDead`,SW 原生 dead 判定照搬:`rewardForLiving/(Living+Dead)` 权重掷活捉/尸体);支付(扣银→死亡则 Kill 取尸体、活捉则断腿+麻醉→轨道家空投舱否则 `IncidentWorker_Visitors.SpawnVisitors`)/拒付(-100 好感+袭击)/宽限 24h(postponedWarrants)三选项语义与 SW 逐行对应;`Dialog_NodeTreeWithFactionInfo`+`ArchivedDialog` 照旧。
- **发单失败**(`HandleFactionsTakenWarrants` **前置快照+后置 diff**,不替换方法):后置里对"离开 takenWarrants 且不在 createdWarrants(弃单回池)/postponedWarrants/本 tick 已弹窗(MakeWarrantDialog prefix 登记名单)"的单子补发 `Tale_IssueFail_0..4` 故事信(八期扩 3→5;NegativeEvent,关联 accepteer);SW 原有的猎手敌对弃单回池、失败好感扣减原样保留。
- **接单方(玩家交货)**:四张 QuestScriptDef(Script_WarrantPawn/Animal/Tame/Artifact)——①成功分支 `QuestNode_End(sendStandardLetter)` 改为 `QuestNode_Signal(victim/artifact.WarrantRequestFulfilled)`→[故事信(PositiveEvent,`Tale_TakeSuccess_*`,slate 变量 [victim_nameFull]/[asker_faction_name]/[reason]/[rewardForLiving] 等直接入文)+`QuestNode_End(Success)`](原版 BanditCamp 同款模式);②两条过期失败分支(站点消散/任务超时)TKey 从原版共享键(LetterLabelQuestExpired 等)换 `RK_Bounty.Tale_TakeFail_*_Label/Text`+`letterDef NegativeEvent`——**不占原版共享键,避免污染其它任务**。
- **文案组织**:变体靠 C# 端 `Rand.RangeInclusive` 选键(接单方无变体,靠 slate 变量名/罪名/派系随机化);三语目录(CN×2 同文/EN)各 113 键(八期后);TKey 在任务生成期解析,def 内英文仅作兜底。
- **改 script def 用 python 正则而非 Edit**(TKey 替换+letterDef 同缩进插入+成功分支整块替换),完成后 ET.parse 校验;首次跑挂在一处:插入 letterDef 时吞掉 `</li>` 未回写 → mismatched tag,替换串必须把消费的闭合标签补回。
- **八期·执行进度故事信**(`BountyRadioManager.ScanBountyProgress`,挂既有 60 tick 轮询,零新 Harmony 热路径):仅跟踪 `issuer==OfPlayer` 的 takenWarrants,按通缉 `GetUniqueLoadID()` 存 `bountyProgress` 字典(loadID→里程碑 0接单/1追踪/2接火)随存档 Scribe;新 ID 入 taken → 接单信;`(now−acceptedTick)/(tickToBeCompleted−acceptedTick)` ≥0.35 → 追踪信、≥0.7 → 接火信(各两变体 `Tale_Accept/Track/Clash_*`,NeutralEvent,占位 THING/FACTION/TARGETFAC);ID 离开 taken(成功/失败/敌对弃单回池)→ 清跟踪,回池再接单则重走全流程;读档首扫按当前比例静默登记基线不补发过期信。同批把发单成功/失败变体池 3 → 5(`Tale_IssueSuccess/Fail_3/4`,Rand 上界 2→4)。
- **⚠⚠ 二次翻车(八期后游戏实测,2026-08-31)**:引导修好、PatchAll 真正跑起来后,立刻暴露六期「发出端」补丁**声明类写错**——`TryAddWarrant(Warrant,string)` 挂在了 `WarrantsManager` 上,实际它是 **`MainTabWindow_Warrants` 的 private 实例方法**(悬赏主窗「发布通缉」按钮路径)→ `Undefined target method` → **PatchAll 半路中断,排其后的补丁全部落空**(表现:发布无拦截/无预付/无门槛,但日志只有一条启动期错误)。修复:①目标类改正为 `MainTabWindow_Warrants`;②引导改为**逐类打补丁+try/catch 隔离**(失败者 `Log.Error("QuestBoardHSK 补丁失败 X")` 后继续其余,一枚失配不再连坐全程序集)。教训链:**编译通过≠已应用;PatchAll 没报错≠全部打上——每次改动必须翻启动日志查 patch 失败**;Harmony 前置参数按名匹配目标参数,声明类要逐一对照反编译源。
- **SW 原生 UI 提示非 bug**:悬赏窗里报酬行旁的红 X = `InsufficientRewardIcon`,条件 `issuer.IsPlayer && MaxRewardValue() < 目标身价×0.75`(SW.InsufficientReward)——玩家自建悬赏报酬是手填的,出价低于身价 75% 就亮 X,与本地补丁无关。
