# 石化线 v3 · 本轮清除清单（2026-08-29 收尾审计）

> 口径：对 `HSK工业科研大修` 工作区 + 部署 `Mods/` 双侧，v2→v3 彻底化改造的**删了什么 / 改了什么 / 还留什么风险**。
> 基准备份：`_tmp/petro_v3_backup_20260829/`（298 项，含 workspace + deploy 两份）。

## 一、清除（退役，不留残体）

| 类别 | 内容 | 判据 |
|---|---|---|
| def 17 个 | 建筑 7（DeepCracker / FractionatingColumn / CatalyticReformer / GasBoiler / OilCracker / AmmoniaReactor / NeutroamineSynthesizer）+ 储罐 7（AsphaltTank / NaphthaTank / NaturalGasTank / AmmoniaTank / NeutroamineTank / NaphthaTankVertical / NeutroamineTankVertical）+ 研究 3（DeepCracking / FractionationReform / Ammoniation） | 相对备份净删 |
| v2 删除清单 31 项 | 上述 17 + 物品 6（Naphtha / MixedGas / Toluene / Synthylene / Synthamide / GasCylinder）+ 配方 8（DeepCrack / CrackSyn / Frac_Olefins / Frac_Aromatics / Reform / Ammonia / Neutroamine / CrackWeave） | **全 mod XML+CS 精确边界 grep = 0 命中**（不是前缀命中，`…TankVertical` 不误算） |
| 补丁 1 个 | `Patch_RK_Petro_AsphaltIcon.xml`（沥青换皮后不再需要） | — |
| 源码死路 | `PetroGasNetHSK.cs`：`PetroGasUtil.CylinderDef`、`GasNetwork.SiphonAdjacentCylinders`、`CompGasPort.CompGetGizmosExtra`(灌装气瓶按钮)、`CompGasPort.ResolveFillIcon`、`CompProperties_GasCylinder`、`CompGasCylinder`、`Command_GasFill` | −7.1 KB；DLL 类型表复核 `CompGasCylinder` 已不存在 |
| 卧式罐 `_south` 图标依赖 | `ResolveFillIcon` 读 `RK_Petro_{NaturalGas,Ammonia}Tank_south`（已删的卧式罐图）随 Gizmo 一并退役 | — |
| 死贴图 68 张 | 已删建筑/储罐/物品图 53 张 + 卧式四向罐 12 张(前缀包含 `Vertical` 被兜底误留，二次精确清) + `_tmp_cut/` 草稿 8 张 + `_tmp_RK_Petro_GasGenerator` + 未挂载的 `GasValve_*_Off` 2 张 + `RK_Petro_NaturalGas/` 目录式残骸 3 张(def 已改 Graphic_Single) | 逐张按 `refs ∪ 变体基名` 精确判未引用 |
| mod 根杂项 | `_bp_seg.txt`（PF 融合期的 def 片段草稿）、`Textures/Things/Building/_tmp_cut/` 空目录 | 移入 `_tmp`，mod 目录内不做任何删除 |
| About 依赖 | `loadAfter` 删 `ratkinpatch.PollutionUnify`（该 mod 已不在 ModsConfig，整并完毕） | ET.parse 通过 |

## 二、改造（改挂/重写）

| 项 | 从 | 到 |
|---|---|---|
| 管网研究引用 ×6 | `RK_Petro_DeepCracking`（已删） | 天然气侧 4 件→`RK_Petro_AtmosphericDistillation`；氨侧 4 件→`RK_Petro_Synthesis` |
| 子分类 defNames ×3 | `RK_Petro_GasPipe_Amm` / `_GasPump_Amm` / `_GasValve_Amm`（悬空） | 改 `_Ammonia` 真名，并补 2 个出料口 → 8 条全部可解析 |
| `Patch_RK_Petro_RemoveOldRoutes.xml` | 子分类里塞 8 个已删建筑 + 5 个已删储罐；注释还写"改挂精馏塔/氨合成塔" | 重写：只列现存 def；注释同步 v3 事实；4 条删除操作全部 Conditional 门控 |
| 卸货湾 `RefineryLoadingBay` | 过滤器只有原版 8 种；贴图是 Rimefeller 警戒板 | 补 Vile 新产物 6 种（逐 def 门控）+ 出料口换皮为漏斗图 |
| 蒸馏塔 comp | def 已写 `BlueprintUnlockHSK.CompProperties_WasteAccumulator` 但 **DLL 里没有这个类**（必然加载报错） | 实现完整：`PostExposeData` 存档、槽满挂起 `Bill.suspended`（只挂自己挂过的，不擅自恢复玩家手动暂停）、清理按钮按 `stackLimit` 分堆落地、落不进的不凭空消失、`PostDestroy` 被击毁即洒污（与毒废包语义对齐）、inspect 显示份数+下一份百分比 |
| `CompProperties_GasExplosive` | DLL 里有类、**全 mod 0 引用**（死代码） | 挂上天然气侧管/阀/出料口（nominalUnits 6）与气泵（30）；氨侧维持 GasToxic 毒云不炸 |

## 三、新增

两台 clone 精炼厂 · 两个管网出料口 · `RK_AsphaltRoad` 地面 · 核废料→废料包配方（Def 级 MayRequire）· 防护统一补丁 · 8 张管网/阀门/出料口贴图 · 3 个石化 patch 文件（LoadingBay / ProtectionUnify / 重写版 RemoveOldRoutes）。

## 四、归档位置（一律不删）

| 目录 | 内容 |
|---|---|
| `_tmp/petro_v3_dead_tex_20260829/` | 死贴图 68 张 + 空目录 |
| `_tmp/petro_v3_deploy_removed_20260829/` | 部署侧回收 56 项（上一会话删的 def/贴图当时未同步） |
| `_tmp/petro_v3_tex_backup_20260829/` | 被覆盖前的 6 张自绘管网贴图 |
| `_tmp/petro_v3_scratch_20260829/` | `_bp_seg.txt` + 2 张 `_tmp_cut` 草稿 |
| `_tmp/petro_v3_backup_20260829/` | 施工前全量 298 项（workspace + deploy） |

## 五、验证矩阵（全绿）

| 检查 | 工具 | 结果 |
|---|---|---|
| XML 良构 | `_tmp/petro_v3_audit.py` | 150 文件 **0 失败** |
| 真引用字段悬空（researchPrereq / recipeUsers / thingDefs / costList / defNames / designatorDropdown / wasteDef …） | 同上，基准=本 mod def ∪ 222 个已启用 mod 抽象名 ∪ Unified 27080 def | **0** |
| `ParentName` 抽象基类可解析 | 同上 | **0 不可解析** |
| 本程序集 `Class=` 命中 DLL 类型表 | 同上 + dnfile | **0 不匹配**（115 类型） |
| 本 mod 石化贴图缺文件 | 同上（含 `_east/_a/_m` 变体式判定） | **0** |
| 汉化键悬空 | 同上，扫 5440 个 DefInjected 文件 | **石化线相关键 0**；"其它存量"92836 条为汉化包里 `IfModActive` 门控的未装 mod 键，属既有设计、非本轮范围 |
| v2 删除清单 31 项残留 | 精确边界 grep（XML+CS 全量） | **0 命中** |
| 补丁模拟 | `_tmp/patch_simulator.py` × 5 | 失败 **0**（LoadingBay 13 步全命中 / ProtectionUnify 2 / RemoveOldRoutes 子分类 2 Add 命中，4 条删除 Conditional 因 Unified 已是删后态静默跳过 = 幂等正确） |
| DLL | `build.ps1` rc=0；dnfile 复核 | `CompWasteAccumulator` 在、`CompGasCylinder` 无 |
| 双目录同步 | `_tmp/petro_v3_sync.py`（逐文件 `.sync_tmp`+`os.replace`，sha256 双向） | 1297 ↔ 1297，**差异 0、残留 0、无嵌套目录** |

## 六、残留风险（明确不清的）

1. **`ChemfuelValve_Off` / `DeepchemValve_Off` 未挂载** —— RimWorld 无"flickable 建筑自动换开/关贴图"机制，要自定义 `Graphic` 类才用得上。图已归档，需要时再立项。
2. **卸货湾换皮是全局的** —— `RefineryLoadingBay` 是 Rimefeller 公共建筑，换皮后 Rimefeller 自己的 8 座精炼厂出料口也变漏斗。若不接受，删 `Patch_RK_Petro_LoadingBay.xml` 第一个 Operation 即可（过滤器扩展不受影响）。
3. **合成氨/中性胺双来源** —— 本 mod 两台 clone 厂挂石化 III；Rimefeller 原版 `SyntheticAmmoniaRefiner`/`NeutroamineRefiner` 已被 Vile 改产丙烯/丁二烯，**不再**产氨和中性胺，所以实际不冲突。但若哪天 Vile 撤掉那层 patch，会出现两条来源。
4. **`ProcessorFramework.dll` 保留** —— 见施工图 §4-1，约束「勿删」。
5. **三燃料精炼塔 / 润滑油 / 中性花瓣** —— 仍是草案，见施工图 §6。
6. **Unified.xml 基线过期** —— 现那份是 08-29 10:26 导出的，不含 v3 新 def；本轮审计里"本 mod 自身 def"走的是实扫，不受影响，但下轮若再以 Unified 为唯一基准会漏判。

## 七、冷启动 Player.log 关键字清单

启动后按顺序 grep（`%USERPROFILE%\AppData\LocalLow\Ludeon Studios\RimWorld by Ludeon Studios\Player.log`）：

| 关键字 | 命中意味 | 定位方向 |
|---|---|---|
| `Could not resolve type` / `CompProperties_WasteAccumulator` | 油泥槽类没解析到 | DLL 未同步 / Assembly 名不符 |
| `Could not find a def named` + `RK_Petro_` | 新 def 引用了不存在的 def | 查 `Buildings_RK_Petro_Refineries.xml` / `Recipes_RK_Petro.xml` |
| `Failed to find a node` | 补丁 xpath 打空 | 看上一行的 xpath 与文件名 |
| `Error while loading` + `RefineryLoadingBay` | 卸货湾换皮/过滤器补丁异常 | `Patch_RK_Petro_LoadingBay.xml` |
| `duplicate unlocked defs` | ResearchTreeSK 重复解锁 | 蒸馏塔 8 档 bill 同挂一个研究 |
| `has a lower techlevel than` | 科研越级 | 跑 `_tmp/check_techlevel_full.py` |
| `Xylene`/`Coke`/`Butadiene`/`Propylene`/`HMFibers` + `Could not find` | 卸货湾过滤器引用了未装 mod 的产物 | 应为 Conditional 跳过；若报错说明门控失效 |
| `Root level error in OnGUI` / `BeginScrollView` / `Mouse position stack` | GUI 栈泄漏（Assemblies 残留同名单 DLL 的典型征状） | 查 `1.6/Assemblies` 是否多出 dll |
| `NullReferenceException` + `DrawPlaceMouseAttachments` / `Widgets.ThingIcon` | stuffable 建筑缺 `uiIconPath` | 本轮两台精炼厂已改纯 costList 规避；若复现查新加建筑 |
| `Could not load Texture2D at 'Things/Building/Linked/RK_Petro_GasPipe` | 管网图集路径/尺寸问题 | 核 `Textures/Things/Building/Linked/` |
| `Could not load Texture2D at 'Rimefeller/Things/Building/` | 精炼厂跨 mod 引用取不到图 | Rimefeller 未启用或加载序变了 |
| `already used in this mod` | 抽象基类 `Name=` 重名 | 新增基类前 grep 全 Defs |
| `Missing ModDependency` / `Ratkin Patch` 相关红❗ | 加载序问题 | 本 mod 未加 HSK 链尾巴类框架 mod 声明 |

游戏内另需肉眼确认 5 点：油泥槽累积与停机、精炼厂吐料到卸货湾、管网分色连通、沥青路面出现在"自然地板"且造价沥青×2、Rimatomics 机加台出现核废料配方。
