# Hardcore SK + Vile 更新记录(2026-08-30)

> 上游快照 `d6c6664`(HSK 全仓库,`version_hsk.txt = hcsk 1.6.4518 / rev 84`)+ `Viles_20260830`。日志区间 HSK `57697edb..d6c6664`、VilesMods `3dbbac9d..b24ed2ad`。上次更新见 `HSK更新记录_20260828.md`。
> 本次为**全仓库快照**(非差分包),但仍按增量处理:只覆盖/新增,不删部署端独有文件。

## 1. 落盘范围

| 项目 | 值 |
|---|---|
| 覆盖 | 174 个文件 |
| 新增 | 72 个文件 |
| 跟进上游删除 | 1 个(`HMC Vile's Pre-Industrial/Patches/Subcategory_Patch.xml`) |
| 跳过 | 2 个(`CombatExtended/.gitattributes` VCS 文件、`BackUpPower/Languages/ChineseSimplified/Keyed/BackupPower.xml` 本地译文更完整) |
| 波及 mod | 42 个(部署侧全部已存在,无新增整包) |
| 回滚备份 | `_tmp\backup_上游更新_20260830\`(按 `包名/mod/相对路径`;被删文件在 `_deleted/`) |
| 脚本 | `_tmp/upstream_20260830/` 全链可重跑:`diffcheck`(行尾归一差分)→`zh_attrib`(汉化键级归属)→`apply_20260830`(dry-run/`--apply`,`.newtmp`+`os.replace`,md5 断言)→`verify_final`(终态核对)→`cycle2`(Tarjan 找环) |

落盘后复核:**md5 失败 0、仍不一致 0**。

## 2. 真变更(行尾归一后)

原始 2256 文件差异多为 CE 全库 LF 归一的假差异;归一后 42 个 mod 有实质变更,核心项:

| mod | 变更 |
|---|---|
| **Core_SK** | `Core_SK.dll`/`SkyAiRefined.dll` 更新;`WorkTypes_UF.xml` **注释掉 `Misc`、`Processing` 两个 WorkTypeDef**(只剩 `BiotechHauling`);`WorkGivers_SK.xml` 一个武器制作 WorkGiver `Crafting → Smithing`;`SK_Patches_RecategorizedWorkGiver.xml` 注释掉一个 Replace;新增 `Duties_InsectoidSwarm.xml` + `RaidStrategies_InsectoidSwarm.xml`,改 `Factions_Insectoids.xml`/`Races_Insectoid.xml`(虫群专用突袭 AI);`AutoGenPatches/MeleeWeapons.xml` 仅给突破斧补 `WorkToMake 5000` |
| **Core_SK_CoreModule** | `Patches/Core/WorkGivers.xml` **取消注释** → `DoBillsCremate` 的 `fixedBillGiverDefs` 重新含 `Kiln`(窑炉火化账单) |
| **Core_SK_DLCModule** | `Biotech/Patches/WorkType/Mechanoid.xml` 注释掉 allowedWorkTypes 里的 `Misc`/`Processing` |
| **Core_SK_Stratum** | 0.11.0→0.12.21:`LoadFolders.xml`、屋顶/悬挂设施/WorkGiver/教程全量刷新;新增 `Compat/ferny.BetterArchitect/`、`Compat/skyarkhangel.hsk/1.6_Late/Patches/Stratum_PostPatches.xml`;净增 3 个太阳能屋顶 def |
| **CombatExtended** | 新增 `ModPatches/Save Our Ship 2/`(12 文件 + `SOS2Compat.dll`)。本机未装 SOS2 → 不激活 |
| **HSK Autosort and Mod Assistant** | `ModListerSettingsDefs.xml` 61205→60611,**移除 `vr.missilegirl` 与 `gwinnbleidd.mothballedanddeadpawns` 两个映射条目**(两者仍在 ModsConfig 启用) |
| **Faster raids progression** | 新增 `RatkinRaceHSK_Faction_Patch.xml`(用 `PatchOperationReplaceExtended Merge`,老的 `pawnGroupMakers/li[8]` Replace 已注释);`HSK_Faction_Patch.xml` 10745→11154。About 增 `loadAfter skyarkhangel.HSK.Core` |
| **Vile's Amuse Bouche** | `OpenHearth`/`MakeFlour`/`MeatSubRaw`/`DoBillsBake`/`DoBillsGrindStone`/`DoBillsKitchenTable` 6 个 def **从 Vile 迁到 Core_SK 提供**(Vile 侧改成稀疏 Patches) |
| **Vile's Hardcore Renovation** | `Patches/Doors.xml` 门贴图路径改 HSK 新版 |
| Misc Robots / Androids | `AIRobot_Races.xml` 注释掉 `Processing` 工作类型与一个 li |
| RatkinRaceHSK / Asari / Rimatomics / Rimefeller / SeedsPlease / DBH / STL / Quarry 等 | **纯中文汉化补齐**(见 §3) |

## 3. 汉化键级归属

对 30 个含中文变更的 mod 逐文件比对"部署端独有键 / 包版独有键 / 同键不同值":

- **29 个 SAFE**:部署端独有键 = 0、同键文案完全一致,包版只是补齐原先被注释掉的键(最大 RatkinRaceHSK 40 文件、DBH 28、Rimatomics_SK 23、AsariRace 17、SeedsPlease 15)→ 整文件覆盖无损失。
- **1 个 RISK = `BackUpPower`**:部署端独有 6 键(`ForceFlickOff/On`、`BatteryBackupEnabled` 已有中文)+ 15 处同键不同值且部署端文案更顺 → **跳过覆盖,保留本地版**。
- 另 52 个纯新增中文文件直接落盘。
- 加载序上 `HSK核心汉化 → HSK附属mod汉化 → AI汉化包` 都排在各 mod 之后,上游 in-mod 中文**不会**覆盖本地已定稿文案。

## 4. 对本地 mod 的冲突影响(重点)

### 4.1 已修的 1 处

`1.6HSK核心汉化/{1.4,1.6}/.../DefInjected/WorkTypeDef/WorkTypes_UF.xml` 的 `Misc.*`、`Processing.*` 共 20 行悬空键已删(上游把这两个 WorkTypeDef 整块注释),双目录已同步并 md5 核对。

### 4.2 全量差分核对:本地 mod 的 def 零丢失

用更新前后两份 `MissileGirl/Cache*/Unified.xml`(UTF-8+BOM)按 mod 分组比对 defName 集合:全局 def 条目 30033 → 30030(Δ−3),**只有 Core_SK / Core_SK_Stratum / Vile's Amuse Bouche 三组有变**,其余 136 组(含 `鼠族HSK拓展` 766 条、`鼠族家具拓展`、`美狐`、`库林`、`金鼠族`、`HSK工业科研大修`、`HSK修复整合` 等**全部本地 mod**)**逐组完全一致**。

### 4.3 逐项排查结论

| 排查项 | 结论 |
|---|---|
| `Misc`/`Processing` 停用 | 本地 mod 的 XML/C# 源码零活引用(`grep <workType>(Misc\|Processing)`、`GetNamed<WorkTypeDef>("Misc")` 均 0);`库林战狐HSK拓展` 只用原版 `Mech_Lifter`,不受影响 |
| 92 号鼠族异种池 | `RatkinColonist`/`RatkinColonistSK` 终态仍是 **8 键、sum=1.000、首键 `RK_XenoType_Ratkin 0.80`**;本次 RatkinRaceHSK 只动中文,没再动 `xenotypeChances` → 08-28 那次"count=4 选择器双头失效"不复发 |
| Faster raids 新增鼠族补丁 | `PatchOperationFindMod` 匹配 `NewRatkinPlus`(= RatkinRaceHSK 的 About `<name>`),生效;只改 `earliestRaidDays 96`、袭击曲线、`RatkinTunnel_Guerrilla.earliestDay 245`,不碰 `caravanTraderKinds` → 与 `67_蓝图学者商队挂载.xml` 无字段交集。部署侧遗留的 `.disabled`/`.bak` 后缀不被加载,保留无害 |
| HMC `Subcategory_Patch.xml` | 该补丁用 Replace 把 `SubCategory_Conversion/defNames` 硬压成 5 项,会抹掉 Core_SK 已有的 `CharcoalPit/CompostBarrel/BurnPit/BiosolidsComposter` → 随上游删除。终态核对:该子分类 16 项、4 个新建筑齐全(16 含其它 mod `Merge=true` 追加与少量重复,非本次引入)。本地只在汉化里注 `SubCategory_Conversion.label`,不受 defNames 影响 |
| Amuse Bouche 6 def 迁移 | 6 个 defName 全部由 Core_SK 继续提供(`Buildings_Production_Food`/`Recipes_Meals_Ingridients`/`WorkGivers_SK`/`ThingCategories_SK`)→ 本地 4 处引用(`酒馆工具整合HSK/TallowAsFuel`、`HSK修复整合/07_配方与工作台`、`鼠族家具拓展/风格基础件风格化`、`工作动画HSK/Compat_CombatExtended`)目标仍在,且都是 Conditional/Add,最坏静默跳过 |
| 窑炉重新挂火化账单 | `DoBillsCremate.fixedBillGiverDefs` 终态含 `Kiln` ✅。注意历史坑:Kiln 在 Core_SK 是 `UniversalFermenterSK.ThingDef_UF`,普通 RecipeDef 挂它会逐条刷 `should have Class=RecipeDef_UF` —— 本次冷启动该签名 **0 命中**,未复发 |
| ITab_Bills 并入 `SK_WorkTable` | 本地 `<inspectorTabs><li>ITab_Bills</li>` 都在自建工作台 Def 上,没有对上游炉灶 Add ITab_Bills 的补丁 → 不产生重复 |
| `Pot`(Vile's Metallurgy 改为覆盖 Core_SK) | 本地零引用 |
| 近战武器完整 Def 迁移 | `AutoGenPatches/MeleeWeapons.xml` 新旧只差一行 `WorkToMake`;`93_工具_科研清理与数值.xml` 引用的 `MeleeWeapon_Sledgehammer/Monkeywrench/Bonesaw/Knife` 等仍定义在未改动的 `Weapons_Melee_*.xml` → 本批安全,后续批若迁移这些完整 Def 需复查 |
| ModLister 映射表瘦身 | 14 个进表的本地 mod 条目**全部保留**;上游净删 8 条 About 排序边(KingFisher 去 loadAfter harmony/prepatcher/PurePatcher/DBH + loadBefore 4 DLC;MissileGirl 去 loadAfter 4 个 EndMod;AlienRaces 增 loadAfter XML Extensions),5 个 packageId 全未变 |

## 5. 冷启动验证

强杀 PID 42220 → 落盘 → 同步汉化包 → 轮换 `MissileGirl/Cache`→`Cache_bak_20260830_190235` → 启动 → 等 def 终态化完成。

- 已知故障签名全 0:`Config error`、`lower techlevel`、`should have Class`、`duplicate unlocked defs`、`already used in this mod`、`Mouse position stack`、`NullReference`、`InvalidProgram`
- `[HSK Autosort & Mod Assistant] patches loaded successfully`;`HSK Core :: Error Check Finished.`
- 与更新前日志做**错误签名差集:新增 0 类**(仅消失一条 UXE `DirectoryNotFoundException`,属导出路径历史问题)
- 排序:`_tmp/loadorder/check_order2.py`(复刻 `ModHasAnyOrderingIssues`)**flagged 0**;`cycle2.py`(读正确 ModsConfig、只算启用 mod)**环 0**(前后均 0;`_tmp/loadorder/find_cycle.py` 报的环是它 CFG 路径写错、把未启用/工坊副本算进来的假阳性)
- 中文报告 `Simplified Chinese has 9629 errors`(更新前 9288,+341)—— 增量来自上游新落的 52 个 in-mod 中文文件里指向未装 DLC/mod 的键,属上游自带噪声,不影响加载

## 6. 遗留观察项(2 已回退)

1. **`BlueprintUnlockHSK.BlueprintUnlockInit` 静态构造抛 `HarmonyException`**(`EmitCallParameter`→`AddPostfixes`,目标 `static IEnumerable<Verse.Thing>`)。**更新前日志同一处已报错**,非本次引入,属 `HSK工业科研大修` 自身补丁签名失配,会连带该 DLL 整个 PatchAll 失败。
2. **已回退(08-30)**:映射表 2 行 + KingFisher/MissileGirl 被上游删的 About 排序边按 `_tmp/backup_上游更新_20260830/` 还原,`vortex.kingfisher` 回到 ModsConfig 第 6 位;`check_order2` flagged 0、环 0。
3. `Vile's Materials Science`/`Metallurgy` 等其余 Vile 包与部署端**完全一致**(日志所述稀疏迁移已在 08-28 批落过),本次 0 变更。
4. `Core_SK_Stratum` 部署端多出 6 个俄文 Keyed(上游已删),按"部署端独有保留"未动。

## 7. 注意事项

含 `Core_SK.dll`/`SkyAiRefined.dll`/`SolarWeb.Stratum.dll` 更新 → **必须完全重启游戏**(本次已重启并验证),旧存档无需重建。上游 mod 只落部署目录不进工作区;本次工作区唯一改动是 §4.1 汉化包 2 个文件,已双目录同步。
