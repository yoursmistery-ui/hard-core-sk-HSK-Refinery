# 全量漏译排查与补译_20260827

> 范围: `RimWorld\Mods`(含 workshop 定位) + `Data` 的简体中文加载链。工具链: `_tmp/transl_scan/`(scan7 + build_library + classify2 + apply_B_C + verify_zh + gaps2)。

## 一、扫描方法论(两条关键修正)

1. **加载根必须复刻游戏回退规则**。`ModContentPack.InitLoadFolders`(反编译):有 `LoadFolders.xml` 且命中版本节 → 只按声明加载(不加载根目录);**没有** `LoadFolders.xml` → 自动加载 `<root>/<版本号>` + `Common` + `<root>`。
   旧扫描漏了回退分支,导致 `1.6HSK核心汉化`(无 LoadFolders)整个 `1.6/Languages` 被误判为"未翻译",虚报 ~150 条。
2. **判"已译"不能看缓存值**。`Unified.xml`(缓存与 Mods 根那份)都在**翻译注入之前**导出,只有少数被 mod 直接改写的 def 显中文。唯一可靠依据是"中文语言根里是否存在该键"。

## 二、本轮修掉的真问题

| # | 问题 | 影响 | 处理 |
|---|---|---|---|
| 1 | **金鼠族 HSK版本** 把 AlienBackstoryDef 汉化放在 `1.6/Languages/ChineseSimplified/DefInjected/BackstoryDef/`,而 320 个背景全是 `AlienRace.AlienBackstoryDef` | 941 个键(title/titleShort/description)**一条都没生效**:创建角色/人物信息页里金鼠族儿童·成人背景全是英文 | 全部重定位到 `Languages/ChineseSimplified (简体中文)/DefInjected/AlienRace.AlienBackstoryDef/OARatkin_Backstory.xml`(根目录优先级最高),并合并 18:41 手工补的 `AI补译.xml`(10 处异值以人工版为准) |
| 2 | **美狐HSK拓展** `LoadFolders.xml` 未声明 `<li>/</li>`,根 `Languages/` 永不加载 | 27 条弹药/特效/基因/种子中文(含今晚 18:41 补的)是死内容 | 内容并入已加载的 `HSK/Languages/ChineseSimplified (简体中文)/DefInjected/...`,根文件清空留指针注释 |
| 3 | 150 条"有英文原文但完全没有汉化键"缺口 | 工作项 verb/gerund、VEF 作业名、CE 12 号口径弹药、边境拓展世界目标/建筑、HSK工业科研大修事件名、酒馆/书籍作业名等 | 人工翻译 + 复用 CE/HSK 既有中文,写入 `translations_master.TRANS`,按路由落盘 |
| 4 | AFU发型HSK适配 119 个发型 label 是 `AFUf001` 内部编号 | 角色编辑器/发型列表显示内部编号 | 按 性别+长度+脏辫+编号 自动起名(`AFU女式长发-07`、`脏辫·AFU女式长发-02`…);另补 AFU/Gloomy 发型分类名 |
| 5 | 原版 1.6 新增 22 款发型 + TaleDef 官方/AI 汉化包均漏 | 发型选择框显示英文 | 写入 `1.6HSK核心全ai汉化` |
| 6 | 第三方 mod 补译缺口(CombatExtended / Character Editor / Misc Robots / VEF / Rimatomics++ / Allow Tools / HMC USSR …) | 半拉子字段 | 写入 `1.6hsk附属mod汉化/1.4/<对应门控目录>/.../AI补译.xml` |

**结果**:玩家可见的真缺条目 699 → **13**,伪翻译(键在值为英文) → **12**,保留项全部是枪械型号/口径代号/模组名(`AK-47`、`5.56×45mm NATO`、`HugsLib`、`PPHC` 等)。

写盘校验:今晚改动 96 个中文 XML 全部可解析、根节点均为 `<LanguageData>`、**同文件零重复键**、类型目录名全部能解析到真实 def 类型;工作区↔部署逐文件 hash 一致(例外见下)。

## 三、发现的工具链缺陷(已记录)

- 早先 `_tmp/transl_scan/writeback.py` 生成的 `AI补译.xml` **每个键都写了两遍**(同值)。本轮重写的文件已去重为单份;其余 350+ 个历史补译文件仍是双份(同值,不影响游戏,需要时再统一清洗)。
- `_tmp/scan_lang_gap.py` 一类旧脚本没有 LoadFolders 回退模型,复查时勿再用。

## 四、下一轮待办 backlog

`_tmp/transl_scan/work/gaps2.tsv`:**1094 条**完全没有任何汉化键的嵌套字段(键写法 `defName.stages.0.label`,索引为同名 `li` 序号)。

| 条数 | 源 mod | 类型 | 说明 |
|---|---|---|---|
| 476 | 原版文化拓展-迷因与建筑 | ThoughtDef | `stages.N.label/description` 道德思想整个未翻 |
| 88 | Androids | AlienBackstoryDef | title/titleShort/description 全英文 |
| 84 | 原版文化拓展-迷因与建筑 | RitualOutcomeEffectDef | 仪式结果说明文本 |
| 70 | vanilla:Royalty | QuestScriptDef | 任务简报文本 |
| 48 | Cybranian - Diseases+ | ThoughtDef | 同上 |
| 41 | CyberneticOrganism_SK | RecipeDef | `jobString` |
| 25 | VanillaExpandedFrameworkLocal | JobDef | `reportString` |
| 23 | 酒馆工具整合HSK | RecipeDef | `jobString` |
| ~5 | 隐式配方 `ExtractSeed_*` | RecipeDef | `jobString`(运行时生成,静态扫不到) |

## 五、遗留的双目录漂移(非本轮引入,需另案处理)

`特性拓展modHSK` 工作区比部署多出一批 `Source/*.cs`、`表/`、`设计/`、`1.6/Defs/ThoughtDef/Thoughts_Memory_TraitShift.xml`、`Keyed/VTE_TraitLetters.xml`(23:13 刚改过)。本轮同步脚本已显式跳过该 mod,未覆盖。

## 六、追加 2026-09-06：`1trickPwnyta's Defaults`（默认设置）整包补译

根因不是"没汉化"，而是**上游中文只做了 59/179 键**，且该 mod 的设置界面文字全在自定义 def（`DefaultSettingDef`/`DefaultSettingsCategoryDef`/`WorkPriority*Def`，共 84 个带 label/description）里，属 DefInjected 范畴、上游一份未写 → 打开面板满屏英文。

处理（**直接落在原 mod 内**，用户要求不走附属包）：先把 `Mods/1trickPwnyta's Defaults` 整包（454 文件/1.65 MB）收进工作区根目录，改动在工作区做完再逐文件同步回部署。

| 位置（均在 `1trickPwnyta's Defaults/1.6/Languages/ChineseSimplified (简体中文)/`） | 数量 | 说明 |
|---|---|---|
| `Keyed/Defaults_Keys.xml` 并进上游缺的键 | 59 → 179 | 只补不重复；`{0}{1}` 占位符逐键核对；保持原文件 noBOM+LF |
| `DefInjected/Defaults.Defs.DefaultSettingDef/AI补译.xml` | 57 | 抽象基类 `DefaultCareForBase`（只有 `Name=`+`Abstract`、无 defName）不注入 |
| `DefInjected/Defaults.Defs.DefaultSettingsCategoryDef/AI补译.xml` | 16×(label+description) | 分类名与说明 |
| `DefInjected/Defaults.Defs.WorkPriorityConditionDef` / `…EffectDef` | 7 / 4 | 优先级规则条件/效果 |

核对 `_tmp/defaults汉化/step3_verify_sync.py` → **PASS**：ZH 键 179/179、零重复、零占位符异常；四类 def 源/注入数量对齐且无悬空注入；工作区↔部署 458 文件全树 md5 一致。首版曾按常规路由做成 `1.6hsk附属mod汉化/1.6/补译_defaults_1trickpwnyta/` + `LoadFolders` 门控，本轮已整体回退（两副本 LoadFolders 复原一致、无残留门控，包体移入 `.trash/附属包defaults补译回退-20260906/`）。

⚠️ 这是工坊 mod，Steam 更新会**整包覆盖**回英文——工作区那份即为恢复源，上游更新后按 `Source/*.cs` 差异重跑 `_tmp/defaults汉化/step2_inject.py`（幂等，已并过的键自动跳过）再同步。`1.5/` 版本目录未镜像（本环境只跑 1.6，且 def 集合可能不同，注入会产生悬空键）。

残留英文仅 17 处，且抽查后绝大多数其实是 `.Translate()` 的**键名**而非硬编码字面量；About 描述与英文 keywords 按原版保留（搜索已能命中中文 label）。
