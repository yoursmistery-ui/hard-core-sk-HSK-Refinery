# 石化线精简 v3 · 污染统一 mod 整合清单（2026-08-29）

> 背景：`污染统一`（ratkinpatch.PollutionUnify）整体并入 `HSK工业科研大修`（ratkinpatch.hskindustrialresearchoverhaul），之后该 mod 停用/删除。本文列出**并入项、不搬项、重复项**，防止漏搬或重复。
> 前置盘点：`Mods/污染统一/` 全部文件已核对（2026-08-29）。

## 一、并入本 mod 的（新增）

| # | 来源文件 | 内容 | 落位 |
|---|---|---|---|
| 1 | `Defs/RecipeDefs/Recipes_NuclearInterop.xml` | 配方 `Process_NuclearWaste_To_Wastepack`：1 NuclearWaste → 2 Wastepack，挂 `TableRimatomicsMachining` | `HSK工业科研大修/1.6/Defs/Recipes_RK_Petro.xml`（或新文件） |
| 2 | `Patches/Patch_B_ProtectionUnify.xml` | 防护统一：`Apparel_GasMask` 补 `Radiation -0.35`、`Apparel_Respirator` 补 `Radiation -0.35`（equippedStatOffsets） | `HSK工业科研大修/1.6/Patches/`（补 xpath 存在性门控） |
| 3 | `Textures/Terrain/RK_AsphaltRoad.png`（36135 B） | 沥青铺路地面贴图（方案 §五 已定 RK_AsphaltRoad 归本 mod） | `HSK工业科研大修/Textures/Terrain/RK_AsphaltRoad.png` |
| 4 | `Source/AsphaltProducerComp.cs` | 源码留档（**不编译、不挂载**，见 §三-1） | `_tmp/污染统一整合_20260829/` |

## 二、已并入本 mod / 重复（不重复搬）

| 来源文件 | 说明 |
|---|---|
| `Defs/ThingDefs/Items_Asphalt.xml`（Asphalt def） | 已于 2026-08-29 并入 `Items_RK_Petro.xml`，内容一致 |
| `Defs/RecipeDefs/Recipes_Asphalt.xml`（Process_Asphalt_To_Concrete） | 已于 2026-08-29 并入 `Recipes_RK_Petro.xml` |
| `Patches/Patch_AsphaltIncinerate.xml` | 本 mod 已有 `Patch_RK_Petro_AsphaltIncinerate.xml`（同功能） |
| `Patches/Patch_HSKModAssistant标记.xml` | 本 mod 已有 HSK 标记，无需搬 |
| `Defs/ThingCategoryDefs/ThingCategories.xml`（PetroResidue） | 未被任何 def 引用（Asphalt 实际归 `RK_Pollutants`），不搬 |
| `Assemblies/PollutionUnify.dll` | 全局 0 引用（见 §三-1），不搬 |
| `Textures/Things/Item/Resource/Asphalt/Asphalt.png` | 本 mod 沥青改用油桶单桶图（`RK_Petro_AsphaltBarrel_a`），此图不使用；如需可留档 |

## 三、特殊说明

1. **`CompAsphaltProducer`（裂化装置副产沥青）**：反编译/Unified.xml 实测 `CrudeCracker` 未挂此 comp、全局 0 引用 → DLL 是"死代码"。且 v3 方案蒸馏塔 8 档已直接产沥青，沥青来源已覆盖。**源码留档，不搬 DLL、不挂载**。
2. **`Process_NuclearWaste_To_Wastepack` 配方**：涉及 Rimatomics（Dubwise.Rimatomics 已启用）。并入时 recipeUsers 引用 `TableRimatomicsMachining` 已确认存在。
3. **`Patch_B_ProtectionUnify`**：目标 `Apparel_GasMask`/`Apparel_Respirator` 已确认存在。并入时按项目铁律补 **xpath 存在性门控**（1.6 起 Operation 级 MayRequire 失效），并保留两个独立 Operation。
4. **铺路贴图迁移**：源在 `污染统一/Textures/Terrain/RK_AsphaltRoad.png`，迁入本 mod 后原文件保留在 `_tmp` 备份（不删第三方目录，遵守"不动第三方 mod 目录"铁律——本 mod 整体整合后由用户决定是否删除污染统一目录）。

## 四、整合后污染统一 mod 处置

- 工作区 `污染统一/` 目录保留（历史），文档标注已并入。
- 部署 `Mods/污染统一/` 由用户在游戏 ModsConfig 停用（本轮**不自动改 ModsConfig**，避免误操作）。
- 若用户确认停用，需核查本 mod 内无对 `ratkinpatch.PollutionUnify` 的依赖/loadAfter。
