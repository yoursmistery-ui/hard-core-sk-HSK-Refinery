## 石化工业线 · 架构总纲（对齐真实终态 · 最终流程图版）
> **⚠️ 2026-08-28 二轮定调：氢气已并入天然气(等价通用, 已落地)** —— 本文"新建清单/缺物"中的氢气物品、氢气管网、氢气罐、氢缩放爆均已删除; 贴图改用户 AI 原图切图(储罐 Graphic_Multi 四向)。现状一律以《石化工业线_全流程框架.md》为准。

> **08-28 更正定调**：本环境**已无 VCE 依赖、也非"从零手搓"**——你早已把 **Rimefeller 精炼厂群 + Vile 化学品改名复用**成石化骨架。真正要新建的只是流程图里 ★新增★ 的前/中段装置 + 少数缺失物。取代此前"手搓半部 VCE"的误判（源于我读了过期 Unified）。
> **实现状态（08-28 四轮·泛化）**：应用户需求，气网从"单介质(天然气)"泛化为**通用三介质气管**（天然气/氢气/氨气），介质枚举 `PetroGas`（避开原版 `Verse.GasType`）。**隔离=每管段单介质+分色**：9 座建筑(3介质×管/泵/阀，defName `RK_Petro_Gas{Pipe|Pump|Valve}_{NG|H2|NH3}`)，端口带 `gasType`，并查集**仅同介质 union**（claim 按 gasType 分字典）→ 三气物理分线绝不互串（比单网多通道更稳，省逐格归属推断）。**Sink 双模式**：`FuelPool`(原版 `CompRefuelable`，锅炉/发电机←NG)、`Deposit`(把松气物品按 `depositReserve` 上限丢到装置 footprint 扩2 地上供 bill 搬运——氨合成塔耗氢、中性胺塔耗氨+氢，各按需挂 1~2 个 Sink 端口)。**储罐**：天然气罐(Source·NG)、氢罐(Source·H2、固定 Explosive 换成缩放爆)、氨罐(Source·NH3+Toxic)。**差异化危害**：`CompGasExplosive`(氢节点 PostDestroy→`GenExplosion.DoExplosion(Flame)` 半径=min(max,min+perUnit×存量)，罐读现存氢量、管段用 nominalUnits)；`CompGasToxic`(氨节点 PostDestroy→`map.gasGrid.AddGas(cell, GasType.ToxGas, amount)` **复用 1.6 原生毒气云范式**，罐按存量扩半径)。**气瓶**：单 def `RK_Petro_GasCylinder` 通用，`CompGasCylinder{petroKind,gas}` 按介质区分，储罐灌装 Gizmo 写源端 kind，`SiphonAdjacentCylinders` 只吸同 kind。玩家主动拆除(`DestroyMode.Deconstruct/Vanish/WillReplace/Refund/FailConstruction/Cancel`)不触发爆/漏。**待游戏内冷启动验证**。

> **实现状态（08-28 三轮）**：在二轮纯 XML 基础上，**管网+断水停机两缺口已由自研 DLL 补齐并部署**（`BlueprintUnlockHSK.dll` 新增 `Source/PetroInfraHSK.cs`+`Source/PetroGasNetHSK.cs`，namespace 复用 `BlueprintUnlockHSK`）。①**断水停机**：`CompNeedsWater`（反射 `DubsBadHygiene.CompWaterStorage.WaterStorage` 低频抽水，未装卫生→永不缺水）+ Harmony 前缀 `WorkGiver_DoBill.JobOnThing`（工作台缺水不发加工单）+ 锅炉/发电机缺水 `CompFlickable.DoFlick()` 自动关断、水回补自动重启；9 座装置各挂 `<litersPerWorkSecond>0.4`。②**真·地图天然气网络**：`MapComponent_GasNet`（footprint 扩 1 邻接并查集分网，%300 重建 / %60 通量，`GasDef` 已缓存避免每 tick 查表）+ `CompGasPort(role)` + 新建筑 **天然气管/气泵/气阀**（研究挂 `RK_Petro_DeepCracking`，二级菜单 `SubCategory_RK_Petro_GasNet`，designationCategory=Rimefeller）。气源=天然气储罐（`role=Source`，读 `AllSlotCellsList`+thingGrid 的松散气物品），用气端=锅炉/发电机（`role=Sink`，`CompRefuelable.Refuel()` 注入现有燃料池，与搬运并存）；气泵通电提升通量（`FlowBase 6 + 每泵 24`/周期），气阀关闭即从网络剔除。③**便携气瓶** `RK_Petro_GasCylinder`（`CompGasCylinder{gas,cap=150}`；储罐上 `Command_GasFill` 灌装指令放罐旁；满瓶放用气装置旁 1 格被 `SiphonAdjacentCylinders` 自动虹吸）。**贴图**：管/泵/阀/气瓶为功能占位图（青=气、红=阀轮），待替换手绘。**仍受限**：储罐仍是 `Building_Storage`（气以松物品存于罐，网络只做无搬运的自动送达，非逐格气压模拟）；爆炸仍固定半径。全部**待游戏内冷启动验证**（清 MissileGirl 缓存）。

> **实现状态（08-28 二轮）**：纯 XML 可玩版**已建成并部署进 `HSK工业科研大修`**（7 物品 + 15 建筑(9 装置+6 储罐) + 15 配方 + 22 贴图，双目录 hash 一致）。本轮补 4 件：①**自有五档科研** `RK_Petro_AtmosphericDistillation/Cracking/FractionationReform/Ammoniation/Polymerization`（Craft_SK 空行 Y=28，全 Industrial 挂 IndustrialBase，入门前置只留 Oil_Industry_C2+Electricity）；②9 座装置接**卫生(Dubwise.DubsBadHygiene)供水管网**、各带 500 L 水箱（卫生原生 `CompProperties_Pipe` mode=Water + `CompProperties_WaterStorage`，Def 级 MayRequire，未装自动跳过）；③卫生自带 `GasBoiler` 由烧化合燃料改烧本线天然气并挂石化 I（补丁 + 附属包中文覆盖）；④补 `RK_Recipe_PetroRubber`（丁二烯+苯→橡胶）兑现工作台"树脂/纤维/橡胶"描述。**仍未做**：储罐是 Building_Storage 搬运式而非管网、爆炸为固定半径、三 comp DLL（5 档分配 Gizmo／抽取→物品／按存量缩放爆炸）、天然气管道泵阀、断水停机（现仅接管+水位，不消耗）。待游戏内加载验证。
> 权威流程图 = 用户最终版（见 `…_最终流程图与可行性.md` 原文）；配方/贴图明细见 `…_产物贴图明细.md`。

---

### 0. 已存在的骨架（实测 Unified + 汉化标签核对）

| 流程图节点 | 实际 def（多为改名复用） | 来源 |
|---|---|---|
| 乙烯 | `Polymers`（已改名"乙烯"）/ `PolymerRefiner`=乙烯精炼厂 | Rimefeller 复用 |
| 丙烯 | `Propylene` / `SyntheticAmmoniaRefiner`=丙烯精炼厂 | Vile + Rimefeller |
| 丁二烯 | `Butadiene` / `NeutroamineRefiner`=丁二烯精炼厂 | Vile + Rimefeller |
| 苯 | `Sulphates`（改名"苯"）/ `SulphateRefiner`=苯精炼厂 | Rimefeller 复用 |
| 二甲苯 | `Xylene` / `ParaffinRefiner`=二甲苯精炼厂 | Vile + Rimefeller |
| 氨 | `SyntheticAmmonia`（改名"氨"）/ `ChemistryLab`=化学工作站 | Rimefeller 复用 |
| 合成纤维 | `SyntheticFibers` / `SynthreadRefiner`=合成纤维合成装置 | Vile |
| 化合燃料/石油/采油 | `Chemfuel`/`CrudeOil`/`OilWell` | Rimefeller |
| 沥青 | `Asphalt`（换皮） | 污染统一 |

**下游消耗端已在**：HDPE/PVC/聚苯乙烯/PET/凯夫拉/诺梅克斯/ABS/有机玻璃/合成橡胶/尼龙/聚碳酸酯/石墨烯气凝胶（Vile+Core_SK 一大片），精炼→工作台→材料这半条链基本成型。

---

### 1. 真正要新建（★新增★，比想象少）

> **归属（08-28 定）：并入 `HSK工业科研大修`（`ratkinpatch.HSKIndustrialResearchOverhaul`），不新建独立 mod**——精简 mod 数。def 放 `HSK工业科研大修/1.6/Defs/`、贴图放其 `Textures/`、补丁放其 `Patches/`、三 comp 放其 `1.6/Source`+`Assemblies`(已有 BlueprintUnlockHSK.dll + build.ps1)。About 已补 loadAfter `Dubwise.Rimefeller`+`ratkinpatch.PollutionUnify`（无环，已验）。

**物品（缺，需自建）**：`甲苯 Toluene`、`氢气 Hydrogen`、`石脑油 Naphtha`、`裂解混合气 MixedGas`、`天然气`（罐体/管网+抽取）、`合成烯 Synthylene`、`合成酰胺 Synthamide`、`精炼X`（若现成精炼厂未覆盖新物）。**氨 `SyntheticAmmonia`、中性胺 `Neutroamine` 均已存在**（复用，不新建）——胺类=中性胺，不单造物。

**建筑（★新增★）**：
- L1 **常减压蒸馏塔**（美狐高炉壳）：石油 →(5档) 化合燃料(主) + 天然气/石脑油/沥青(副)。
- L2 **储罐×N + 管网**（复用 PipeSystem 库，仿 Chemfuel 罐体+抽取）：燃油/气/石脑油/沥青/**氨/中性胺** 各罐，抽取→物品。
- L3 **燃气发电机 / 燃气锅炉**（耗管网气/氢 → 电/热）。
- L5 **深度裂解厂**：气+沥青+燃油+石脑油 → 裂解混合气。
- L6 **精馏塔**：裂解混合气 → 三烯三苯（**删除"无机物转化器"旧产 乙烯/丙烯/丁二烯/苯/二甲苯 配方**，来源全部改挂精馏塔，新建 甲苯）。
- L7 **催化重整装置**：石脑油 → 苯/甲苯/二甲苯 + 副产 氢气。
- L8 **氨合成塔**：天然气+氢气 → **氨**（罐体，删旧"化学工作站用煤/焦炭产氨"配方）；**中性胺合成塔**（=胺化反应器）：氨+氢+乙烯 → **中性胺**（罐体，去精炼步骤）。
- L9 **石化工作台**：精炼X 全家 → 合成树脂/纤维/橡胶。**医药产品逻辑完全不动**（本轮只改中性胺 + 氨两条生产线）。

---

### 2. 氮化学改路线（氨 + 中性胺，你已拍·全面精简）

- **胺类 = 中性胺**：不单造"胺类化合物"，氮链终端就是 `Neutroamine`（现成 def，HSK 弹药/药品仍引用，保留物品本体）。
- **氨 `SyntheticAmmonia`**：**删除 `ChemistryLab`(化学工作站) 用煤/焦炭产氨的配方**，改为只从 **氨合成塔**（天然气+氢气）产 → 走罐体。
- **中性胺 `Neutroamine` 全面精简**：**只能从"中性胺合成塔"(=胺化反应器) 产**（氨+氢+乙烯）；**删除** `MakeNaturalNeutroamine`(药品实验室天然路) 及其它任何中性胺来源。
- **去精炼、改罐体**：氨/中性胺**不做"精炼X"步骤**，改成**和化合燃料一样的罐体存储 + 抽取→物品**（复用 PipeSystem 罐 + 抽取 Gizmo）。
- 落地前必扫：`Unified.xml` 全量 grep `Neutroamine`/`SyntheticAmmonia` 引用（电荷弹/药品/中性胺配方/研究解锁），确认删旧配方不产生悬空引用或 duplicate unlock。

---

### 3. 复用框架 vs 自写（省力边界）

- **复用已装框架**（不算装 VCE）：`PipeSystem` 库（Rimefeller 带）建天然气管网；`CompProperties_Explosive` 做储罐/罐子爆炸；现成精炼厂/工作台 def 直接挂新配方。
- **自写小 DLL**（可后置，先纯 XML 起步）：5 档分配 Gizmo、抽取→物品 Gizmo、按存量缩放爆炸。一期用 5 个 bill / 固定大爆炸 / 搬罐配方 顶替。

---

### 4. 落地顺序 · 验证 · 风险

**顺序**：① 沥青换皮+撤 CrudeCracker 副产(零风险,**已完成+同步**) → ② 内容并入 `HSK工业科研大修/1.6/Defs`(不建独立 mod,**About 依赖已补**) → ③ L1 蒸馏塔+石脑油/天然气物品(纯XML) → ④ L2 储罐/管网/罐子(PipeSystem+Explosive) → ⑤ L6 精馏塔把三烯三苯来源从无机物改挂石化 + 新建甲苯(删无机物转化器旧配方) → ⑥ L7 重整+L8 氨合成/中性胺合成+氢气 → ⑦ L9 石化工作台 → ⑧ 中性胺/氨改路线(先扫引用) → ⑨ 小 DLL 三 comp。
**验证**：补丁 xpath 门控；`_tmp/patch_simulator.py`；冷启动读**实时** `Unified.xml`(`MissileGirl/Cache/`，勿用 `Cache_bak_*` 过期备份) 核对新建筑加载/精馏塔改挂成功/无 duplicate·越级；双目录逐文件 hash 同步。
**风险**：① **精馏塔改挂三烯三苯来源**要同时处理"无机物转化器"旧路(保留并存 or 移除)，否则重复解锁/来源冲突；② 中性胺引用面广(弹药/药品)，改前先全扫；③ 甲苯/氢气/胺类是新物，需各自挂生产+消耗端否则死料；④ 我此前用过期缓存误判，**后续核对一律用实时 Cache/Unified.xml**。

---

### 5. 待你定

1. ~~无机物转化器旧路~~ **已定：删除**（三烯三苯来源全改挂精馏塔）。
2. ~~中性胺改路线~~ **已定**：保留 `Neutroamine` 物品、只从"中性胺合成塔"产、罐体+抽取、删化学工作站氨路与天然路。
3. ~~医药产品~~ **已定：完全不动**，本轮只改中性胺 + 氨两条生产线。
4. 新建物品先做哪几个跑通最小闭环（建议：石脑油+甲苯+氢气，先把 L1→L6/L7 打通）？
5. 小 DLL 三 comp 一期上还是二期补？

---

### 6. 贴图需求清单（对齐你已给的 7 张）

**已给 ✅**：常减压蒸馏塔、燃气发电机、中性胺储罐(N-AMINE)、氨储罐(NH₃)、沥青储罐(ASPHALT)、天然气储罐(卧式)、天然气球罐。

**建筑·还缺 🎨**：
- 精馏塔（区别蒸馏塔的另一座塔）
- 催化重整装置
- 氨合成塔
- 中性胺合成塔（胺化反应器）
- 深度裂解厂
- 石化工作台（L9，或复用化学工作站/现成台外观）
- 燃气锅炉（L3，与发电机配套）
- 石脑油储罐、氢气储罐（补齐储罐家族）
- 天然气管道 / 泵 / 阀（若不用 Rimefeller/VChemfuelE 现成管图）

**物品图标·还缺 🎨**：
- 甲苯、氢气、石脑油、裂解混合气、合成烯、合成酰胺（氨/中性胺 复用现成物品图）
- 沥青物品图 ✅（**自绘黑色沥青块 3 段堆叠**，做 Graphic_StackCount；**不再借 MUM 碳纤维图**，避免与碳纤维撞脸）
- 天然气罐子（便携气瓶，若要罐子物品形态）

**可省（复用现成，不用新画）**：石油/化合燃料/钢/塑钢等基础物、乙烯/丙烯/丁二烯/苯/二甲苯 物品图（沿用改名后的 Rimefeller/Vile 现成图）、燃油储罐（Rimefeller `ChemfuelTank` 现成）。

> 建筑贴图落地注意：RimWorld 建筑一般要 **north/east/south/west 四向**（或 Graphic_Single 单向）+ 按占地格数的像素尺寸 + 透明背景；储罐"竖直/水平"两视角可对应 单格/横卧两种建筑或旋转。给图时告诉我每张对应哪个建筑、几格占地，我按 `deploy-sync` 规范放进本 mod `Textures/`。
